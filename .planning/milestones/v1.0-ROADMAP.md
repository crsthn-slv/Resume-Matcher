# Milestone v1.0: Resume-Linked Application Tracker MVP

**Status:** ✅ SHIPPED 2026-03-23  
**Phases:** 1-5  
**Total Plans:** 12

## Overview

This milestone expanded Resume Matcher from resume tailoring into end-to-end application tracking without creating a separate tracker surface. The shipped result keeps the dashboard and resume viewer as the primary workflow, adds a backend application domain with configurable pipeline rules, connects application creation to tailoring, hardens the feature with tests and i18n, and finishes with inline badge-driven status editing.

## Phases

### Phase 1: Applications Backend Foundation

**Goal**: Establish the `application` domain in the backend with durable storage, validated API contracts, status history, and pipeline configuration rules.  
**Depends on**: Nothing (first phase)  
**Plans**: 3 plans

Plans:

- [x] 01-01: Add database table access, schemas, and shared application domain types.
- [x] 01-02: Implement applications CRUD and status-history API endpoints.
- [x] 01-03: Implement application pipeline config endpoints, defaults, and backend tests.

**Details:**

- Requirements: `APPL-01`, `APPL-02`, `APPL-03`, `APPL-04`, `PIPE-01`, `PIPE-02`, `PIPE-03`
- Success criteria:
  1. User can create and fetch application records through the API with optional links to resume and job data.
  2. Status changes only happen through the dedicated endpoint and append correct history entries with timestamps and sources.
  3. Invalid statuses are rejected, and new applications receive the configured default status behavior.
  4. Application pipeline config can be read and updated through backend endpoints with protection against removing statuses in use.

### Phase 2: Dashboard Resume-Linked Experience

**Goal**: Add a usable tracker interface to the existing dashboard by embedding application records into the tailored-resume experience instead of introducing a standalone applications table.  
**Depends on**: Phase 1  
**Plans**: 3 plans

Plans:

- [x] 02-01: Add frontend application API client, types, and dashboard data-loading state for resume-linked application metadata.
- [x] 02-02: Integrate application status, search, and filtering into the existing dashboard tailored-resume entries.
- [x] 02-03: Build linked application details/edit/status flows from the tailored-resume experience.

**Details:**

- Requirements: `PIPE-04`, `PIPE-05`, `APPL-05`, `APPL-06`, `DASH-01`, `DASH-02`, `DASH-03`
- Success criteria:
  1. User keeps using the current dashboard and can recognize application state directly from tailored-resume entries.
  2. User can search by company or role, filter by one or more statuses, and see resume-linked application results ordered by most recent activity.
  3. User can open a tailored-resume item and access linked application details, edit controls, and reverse-ordered history.
  4. Status changes still call the dedicated status endpoint instead of metadata patching.

### Phase 3: Workflow Integrations

**Goal**: Make the tracker part of the broader Resume Matcher workflow through Settings pipeline management and post-tailoring entry points.  
**Depends on**: Phase 2  
**Plans**: 2 plans

Plans:

- [x] 03-01: Build the Settings application pipeline component and integrate config persistence.
- [x] 03-02: Add the post-tailoring application creation flow with linked identifiers and default status behavior.

**Details:**

- Requirements: `SET-01`, `SET-02`, `SET-03`, `TAIL-01`, `TAIL-02`
- Success criteria:
  1. User can manage application statuses from a dedicated Settings component with add, remove, and reorder interactions.
  2. User receives backend-driven error feedback when trying to remove statuses that are still in use.
  3. User can launch application creation after tailoring with `resume_id` and `job_id` prefilled and company/role still editable.

### Phase 4: Quality and Release Hardening

**Goal**: Ensure the tracker is production-ready for this MVP through localization, regression coverage, and UX polish.  
**Depends on**: Phase 3  
**Plans**: 2 plans

Plans:

- [x] 04-01: Add i18n coverage and frontend/backend automated tests for the tracker flows.
- [x] 04-02: Run validation, fix gaps, and finalize MVP release polish.

**Details:**

- Requirements: `QUAL-01`, `QUAL-02`
- Success criteria:
  1. User-facing tracker and settings strings exist across all supported locales.
  2. Backend and frontend automated tests cover the main creation, filtering, status, pipeline, and integration paths from the plan.
  3. The feature passes linting and formatting requirements and fits the established Swiss-style UI patterns.

### Phase 5: Inline application status editing

**Goal**: Replace the passive tracker status signal with a faster inline badge-driven interaction on both the dashboard and resume viewer.  
**Depends on**: Phase 4  
**Plans**: 2 plans

Plans:

- [x] 05-01: Add shared inline status UI/helpers and apply badge-driven status editing on the dashboard.
- [x] 05-02: Apply the same badge-driven interaction in the resume viewer and add focused regression coverage.

**Details:**

- Requirements: none added beyond the shipped MVP behavior
- Success criteria:
  1. Dashboard badges become the primary status interaction.
  2. Unlinked tailored resumes attempt quick-create and otherwise fall back to the normal prefilled create flow.
  3. Resume viewer uses the same badge-driven inline status interaction and removes the old standalone status-change block.

---

## Milestone Summary

**Decimal Phases:**

- None

**Key Decisions:**

- Keep application tracking embedded inside the existing dashboard and resume viewer instead of creating a separate tracker route.
- Centralize pipeline validation in backend config endpoints and keep frontend behavior aligned with backend authority.
- Route tailoring completion into the normal resume viewer so application creation stays resume-centric.
- Use focused helper-level regression coverage for complex tracker logic instead of broad brittle page snapshots.
- Make the visible status badge the primary interaction trigger across dashboard and resume viewer.

**Issues Resolved:**

- Added a full backend application model and pipeline contract.
- Completed dashboard filtering, linked details, Settings pipeline management, and post-tailoring handoff.
- Closed verification and Nyquist validation gaps across all phases.

**Issues Deferred:**

- Standalone applications table or kanban board
- Interview scheduling, reminders, and contacts
- Automatic company/role parsing from freeform job text

**Technical Debt Incurred:**

- None recorded as milestone blockers at ship time.

---

_For current project status, see [`ROADMAP.md`](/Users/cristhian/Downloads/Projetos/Resume Matcher/.planning/ROADMAP.md)_
