---
phase: 01-applications-backend-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/backend/app/routers/applications.py
  - apps/backend/app/routers/__init__.py
  - apps/backend/app/main.py
autonomous: true
requirements:
  - APPL-01
  - APPL-02
  - APPL-03
  - APPL-04
  - PIPE-01
  - PIPE-02
  - PIPE-03
user_setup: []
must_haves:
  truths:
    - Backend exposes application CRUD/status endpoints under `/api/v1`.
    - Application creation enforces strict `job_id` versus `job_description` validation and writes an initial history event.
    - Status transitions are isolated to the dedicated status endpoint and display-ready list responses are returned from `GET /applications`.
  artifacts:
    - apps/backend/app/routers/applications.py defines the applications router and endpoints.
    - apps/backend/app/routers/__init__.py exports the applications router.
    - apps/backend/app/main.py includes the applications router under `/api/v1`.
  key_links:
    - Router handlers use the new database helpers and schema models from Plan 01.
    - `PATCH /applications/{id}` leaves status history untouched while `POST /applications/{id}/status` appends history.
---

<objective>
Implement the core application endpoints and register them in the FastAPI app.

Purpose: Expose the backend contract that later frontend phases and tests will rely on for creation, listing, details, metadata edits, and status changes.
Output: A dedicated applications router wired into the API with strict validation and display-ready list behavior.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-applications-backend-foundation/01-CONTEXT.md
@.planning/phases/01-applications-backend-foundation/01-RESEARCH.md
@apps/backend/app/database.py
@apps/backend/app/routers/resumes.py
@apps/backend/app/routers/__init__.py
@apps/backend/app/main.py
@apps/backend/app/schemas/models.py
@apps/backend/app/schemas/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the applications router with list, detail, create, patch, and status endpoints</name>
  <files>apps/backend/app/routers/applications.py</files>
  <read_first>apps/backend/app/routers/resumes.py, apps/backend/app/database.py, apps/backend/app/schemas/models.py, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md</read_first>
  <action>Create `apps/backend/app/routers/applications.py` with an `APIRouter(prefix="/applications", tags=["Applications"])` that defines `GET /applications`, `POST /applications`, `GET /applications/{id}`, `PATCH /applications/{id}`, and `POST /applications/{id}/status`. Implement explicit validation rules from context: reject payloads that send both `job_id` and `job_description`; create a linked job record when `job_description` is present; verify referenced `resume_id` and `job_id` exist; default the initial status to `Applied` when configured, otherwise the first configured status; append the initial `status_history` entry on create using `manual_create` or `tailor_create`; keep `PATCH` limited to metadata updates without history changes; keep status changes exclusively in `POST /applications/{id}/status` with appended history records.</action>
  <verify>rg -n "@router.get\\(\"\"|@router.post\\(\"\"|@router.get\\(\"/\\{id\\}\"|@router.patch\\(\"/\\{id\\}\"|@router.post\\(\"/\\{id\\}/status\"" apps/backend/app/routers/applications.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/routers/applications.py` contains `APIRouter(prefix="/applications"`
    - `apps/backend/app/routers/applications.py` contains `@router.get("")`
    - `apps/backend/app/routers/applications.py` contains `@router.post("")`
    - `apps/backend/app/routers/applications.py` contains `@router.get("/{id}")`
    - `apps/backend/app/routers/applications.py` contains `@router.patch("/{id}")`
    - `apps/backend/app/routers/applications.py` contains `@router.post("/{id}/status")`
  </acceptance_criteria>
  <done>The API surface for applications exists and enforces the domain rules defined in context.</done>
</task>

<task type="auto">
  <name>Task 2: Make the list endpoint table-ready and queryable</name>
  <files>apps/backend/app/routers/applications.py, apps/backend/app/database.py</files>
  <read_first>apps/backend/app/routers/applications.py, apps/backend/app/database.py, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement `GET /applications` so it accepts `q` and comma-separated `status` filters, orders by `updated_at` descending, and returns application rows ready for dashboard display. Include the application's own fields plus lightweight linked metadata that the table can render quickly, such as linked resume label/title and whether a linked job description exists. Put reusable filtering/sorting helpers in `database.py` if they are needed by multiple route paths; keep one-off response hydration in the router if it is specific to list rendering.</action>
  <verify>rg -n "q:|status:|updated_at|resume_|job_" apps/backend/app/routers/applications.py apps/backend/app/database.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/routers/applications.py` contains query handling for `q`
    - `apps/backend/app/routers/applications.py` contains query handling for `status`
    - `apps/backend/app/routers/applications.py` sorts or returns records by `updated_at`
    - `apps/backend/app/routers/applications.py` returns linked display metadata beyond raw ids
  </acceptance_criteria>
  <done>The list endpoint supports Phase 2's table needs without requiring per-row client fetches.</done>
</task>

<task type="auto">
  <name>Task 3: Register the router in the backend entry points</name>
  <files>apps/backend/app/routers/__init__.py, apps/backend/app/main.py</files>
  <read_first>apps/backend/app/routers/__init__.py, apps/backend/app/main.py, apps/backend/app/routers/health.py</read_first>
  <action>Export the applications router from `apps/backend/app/routers/__init__.py` following the existing alias pattern and include it in `apps/backend/app/main.py` under the same `/api/v1` prefix used by the existing routers. Keep import ordering and router-registration style consistent with the current app entry point.</action>
  <verify>rg -n "applications_router" apps/backend/app/routers/__init__.py apps/backend/app/main.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/routers/__init__.py` imports `router as applications_router`
    - `apps/backend/app/routers/__init__.py` includes `applications_router` in `__all__`
    - `apps/backend/app/main.py` contains `app.include_router(applications_router, prefix="/api/v1")`
  </acceptance_criteria>
  <done>The new router is part of the running FastAPI application and follows existing backend registration conventions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall apps/backend/app`
- [ ] `rg -n "applications_router|/applications|status_history" apps/backend/app/routers/applications.py apps/backend/app/routers/__init__.py apps/backend/app/main.py`
- [ ] Creation, list, metadata patch, and status change flows align with locked Phase 1 decisions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Applications API is reachable, validated, and ready for config/test hardening
</success_criteria>

<output>
After completion, create `.planning/phases/01-applications-backend-foundation/01-02-SUMMARY.md`
</output>
