---
phase: 01-applications-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/database.py
  - apps/backend/app/schemas/models.py
  - apps/backend/app/schemas/__init__.py
autonomous: true
requirements:
  - APPL-01
  - APPL-02
  - APPL-03
  - APPL-04
  - PIPE-01
  - PIPE-02
  - PIPE-03
user_setup: []
must_haves:
  truths:
    - Application records can be persisted with ids, timestamps, status, optional links, and status history.
    - Application creation defaults and status validation are expressible through typed backend models.
    - Database reset clears application records along with the existing tables.
  artifacts:
    - apps/backend/app/database.py contains applications table and helper methods.
    - apps/backend/app/schemas/models.py defines application domain and config models.
    - apps/backend/app/schemas/__init__.py re-exports the new application models.
  key_links:
    - Database application helpers and schema models use the same field names for ids, status, links, and history entries.
---

<objective>
Build the backend foundation for the new `application` domain before any routes are added.

Purpose: Make persistence and typed contracts stable so endpoint work can layer on top without redefining field semantics.
Output: TinyDB application helpers, typed Pydantic models, and reset behavior that includes applications.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-applications-backend-foundation/01-CONTEXT.md
@.planning/phases/01-applications-backend-foundation/01-RESEARCH.md
@apps/backend/app/database.py
@apps/backend/app/schemas/models.py
@apps/backend/app/schemas/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TinyDB application table and lifecycle helpers</name>
  <files>apps/backend/app/database.py</files>
  <read_first>apps/backend/app/database.py, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md, .planning/phases/01-applications-backend-foundation/01-RESEARCH.md</read_first>
  <action>Add an `applications` table property to `Database`, then implement application-focused helpers that mirror the repo's current TinyDB style: create/get/list/update operations, status-history append support, linked-id validation helpers as needed, and default timestamp management with UTC ISO strings. Update `reset_database()` to truncate `applications` in addition to `resumes`, `jobs`, and `improvements`. Preserve current `Database` conventions: use `uuid4()`, `datetime.now(timezone.utc).isoformat()`, `Query()`, and typed Python signatures on every new method.</action>
  <verify>rg -n "def create_application|def get_application|def list_applications|def update_application|def reset_database" apps/backend/app/database.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/database.py` contains `def applications(self) -> Table`
    - `apps/backend/app/database.py` contains `def create_application(`
    - `apps/backend/app/database.py` contains `def get_application(`
    - `apps/backend/app/database.py` contains `def list_applications(`
    - `apps/backend/app/database.py` contains `self.applications.truncate()`
  </acceptance_criteria>
  <done>Database supports the application domain and reset behavior explicitly includes the new table.</done>
</task>

<task type="auto">
  <name>Task 2: Define application record, request, response, and config schemas</name>
  <files>apps/backend/app/schemas/models.py, apps/backend/app/schemas/__init__.py</files>
  <read_first>apps/backend/app/schemas/models.py, apps/backend/app/schemas/__init__.py, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Add Pydantic models for `ApplicationStatusHistoryEntry`, `ApplicationRecord`, `ApplicationConfig`, application list items, create/update/status request bodies, and config request/response payloads. Encode the locked decisions directly in the schema layer where appropriate: optional `resume_id`/`job_id`, optional `job_description`, explicit `status_history`, and typed fields for display-ready list metadata. Re-export every new schema from `apps/backend/app/schemas/__init__.py` using the same import and `__all__` conventions already used for resume/job/config models.</action>
  <verify>rg -n "class ApplicationRecord|class ApplicationStatusHistoryEntry|class ApplicationConfig" apps/backend/app/schemas/models.py && rg -n "ApplicationRecord|ApplicationConfig" apps/backend/app/schemas/__init__.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/schemas/models.py` contains `class ApplicationRecord`
    - `apps/backend/app/schemas/models.py` contains `class ApplicationStatusHistoryEntry`
    - `apps/backend/app/schemas/models.py` contains `class ApplicationConfig`
    - `apps/backend/app/schemas/__init__.py` imports `ApplicationRecord`
    - `apps/backend/app/schemas/__init__.py` lists `ApplicationConfig` in `__all__`
  </acceptance_criteria>
  <done>Typed schema coverage exists for records, create/update flows, status transitions, and config endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Align domain defaults across persistence and schemas</name>
  <files>apps/backend/app/database.py, apps/backend/app/schemas/models.py</files>
  <read_first>apps/backend/app/database.py, apps/backend/app/schemas/models.py, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md</read_first>
  <action>Make the new domain defaults explicit and safe: use a defensive-copy-friendly approach for seeded statuses, keep `status_history` as a per-record list instead of a shared mutable default, and ensure the core application/create models can represent the initial history event from `manual_create` or `tailor_create`. If helper constants are introduced for default pipeline statuses, they must be copied before mutation rather than reused directly.</action>
  <verify>rg -n "status_history|manual_create|tailor_create|Applied|Wishlist|Archived" apps/backend/app/database.py apps/backend/app/schemas/models.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/schemas/models.py` contains `status_history`
    - `apps/backend/app/schemas/models.py` contains `manual_create`
    - `apps/backend/app/schemas/models.py` contains `tailor_create`
    - `apps/backend/app/database.py` or `apps/backend/app/schemas/models.py` contains `Applied`
    - No new mutable list default is introduced without `default_factory` or defensive copy semantics
  </acceptance_criteria>
  <done>The application domain expresses safe defaults and can support the initial audited status event.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall apps/backend/app`
- [ ] `rg -n "applications" apps/backend/app/database.py apps/backend/app/schemas/models.py apps/backend/app/schemas/__init__.py`
- [ ] Domain helpers and schema fields match the Phase 1 context decisions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Database and schema foundation fully cover the `application` record lifecycle needed by later route work
</success_criteria>

<output>
After completion, create `.planning/phases/01-applications-backend-foundation/01-01-SUMMARY.md`
</output>
