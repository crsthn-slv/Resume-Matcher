---
phase: 01-applications-backend-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/backend/app/routers/config.py
  - apps/backend/tests/test_applications_endpoints.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - APPL-01
  - APPL-02
  - APPL-03
  - APPL-04
user_setup: []
must_haves:
  truths:
    - Application pipeline statuses can be read and updated through config endpoints with backend-owned validation.
    - Removing a status in use is rejected with affected application information.
    - Automated backend tests cover manual creation, linked creation, filtering, status history, invalid statuses, and reset behavior.
  artifacts:
    - apps/backend/app/routers/config.py exposes `/config/applications` endpoints.
    - apps/backend/tests/test_applications_endpoints.py verifies the Phase 1 contract.
  key_links:
    - Config endpoints and applications endpoints use the same default statuses and validity rules.
    - Tests assert the create/history/config behavior introduced in Plans 01 and 02.
---

<objective>
Finish the backend phase by exposing pipeline config endpoints and locking the contract with automated tests.

Purpose: Make application status rules durable and safe before any frontend consumes them.
Output: Config endpoints for application pipeline management plus comprehensive backend tests for the new domain.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-applications-backend-foundation/01-CONTEXT.md
@.planning/phases/01-applications-backend-foundation/01-RESEARCH.md
@apps/backend/app/routers/config.py
@apps/backend/app/routers/applications.py
@apps/backend/app/database.py
@apps/backend/tests/test_regenerate_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add application pipeline config endpoints with in-use removal validation</name>
  <files>apps/backend/app/routers/config.py</files>
  <read_first>apps/backend/app/routers/config.py, apps/backend/app/routers/applications.py, apps/backend/app/database.py, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md</read_first>
  <action>Add `GET /config/applications` and `PUT /config/applications` to `apps/backend/app/routers/config.py` using the existing `_load_config()` and `_save_config()` helpers. Seed the application pipeline with `Wishlist`, `Applied`, `Interview`, `Offer`, `Rejected`, and `Archived`, copying defaults defensively before mutation. On update, validate that the resulting status list is not empty, preserve order, and reject removal of any status still referenced by stored applications with a `400` response that includes the affected applications for frontend display. Keep backend-owned status validity logic consistent with the applications router.</action>
  <verify>rg -n "/applications|Wishlist|Applied|Interview|Offer|Rejected|Archived" apps/backend/app/routers/config.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/routers/config.py` contains `@router.get("/applications")`
    - `apps/backend/app/routers/config.py` contains `@router.put("/applications")`
    - `apps/backend/app/routers/config.py` contains `Wishlist`
    - `apps/backend/app/routers/config.py` contains validation for statuses still in use
  </acceptance_criteria>
  <done>The application pipeline can be managed through config endpoints without corrupting existing application records.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend tests for application creation, filtering, status changes, and config safety</name>
  <files>apps/backend/tests/test_applications_endpoints.py</files>
  <read_first>apps/backend/tests/test_regenerate_endpoints.py, apps/backend/app/routers/applications.py, apps/backend/app/routers/config.py, apps/backend/app/database.py, .planning/REQUIREMENTS.md</read_first>
  <action>Create `apps/backend/tests/test_applications_endpoints.py` using the repo's current `unittest`/`IsolatedAsyncioTestCase` style where appropriate. Cover the Phase 1 contract from the milestone plan: manual create with and without `resume_id`, manual create with and without `job_description`, create with `job_id` from tailoring, status change appends correct history, metadata patch does not append history, invalid statuses are rejected, removal of statuses in use is rejected by `PUT /config/applications`, `reset_database()` clears applications, text search matches `company` and `role`, and multi-status filtering works.</action>
  <verify>python -m unittest apps.backend.tests.test_applications_endpoints</verify>
  <acceptance_criteria>
    - `apps/backend/tests/test_applications_endpoints.py` exists
    - `apps/backend/tests/test_applications_endpoints.py` contains a test covering status history append behavior
    - `apps/backend/tests/test_applications_endpoints.py` contains a test covering `PATCH` without history append
    - `apps/backend/tests/test_applications_endpoints.py` contains a test covering blocked status removal in config
    - `apps/backend/tests/test_applications_endpoints.py` contains a test covering search or multi-status filtering
  </acceptance_criteria>
  <done>Backend regression coverage exists for the new application domain and pipeline rules.</done>
</task>

<task type="auto">
  <name>Task 3: Run backend verification and close contract gaps</name>
  <files>apps/backend/app/routers/config.py, apps/backend/tests/test_applications_endpoints.py</files>
  <read_first>apps/backend/app/routers/config.py, apps/backend/tests/test_applications_endpoints.py, .planning/phases/01-applications-backend-foundation/01-RESEARCH.md</read_first>
  <action>Run the backend verification commands for the new application domain, then fix any contract mismatches revealed by the test run or compile step. At minimum, verify router imports remain valid, config endpoints and applications endpoints agree on status rules, and the added tests exercise the final payload/validation behavior rather than stale assumptions from before implementation.</action>
  <verify>python -m compileall apps/backend/app && python -m unittest apps.backend.tests.test_applications_endpoints</verify>
  <acceptance_criteria>
    - `python -m compileall apps/backend/app` exits 0
    - `python -m unittest apps.backend.tests.test_applications_endpoints` exits 0
    - `apps/backend/app/routers/config.py` and `apps/backend/app/routers/applications.py` both reference the same configured status source
  </acceptance_criteria>
  <done>The phase closes with passing backend verification and aligned config/application behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall apps/backend/app`
- [ ] `python -m unittest apps.backend.tests.test_applications_endpoints`
- [ ] Config endpoints, application endpoints, and tests all agree on pipeline defaults and validation behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 ends with enforceable config rules and reliable backend regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-applications-backend-foundation/01-03-SUMMARY.md`
</output>
