---
phase: 04-quality-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/frontend/
  - apps/backend/
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
user_setup: []
must_haves:
  truths:
    - Final release validation runs against the completed tracker MVP and produces evidence-driven fixes instead of speculative redesigns.
    - Any remaining issues uncovered by lint, format, or automated tests are fixed in-place while preserving the resume-centric workflow and Swiss-style UI.
    - The phase ends with the tracker MVP in a releasable state according to roadmap success criteria.
  artifacts:
    - Verification outputs and summary artifacts reflect the final hardening pass.
    - Code changes, if needed, are limited to real issues surfaced by validation or final UI consistency review.
    - Planning state files are updated to reflect release readiness after the phase passes.
  key_links:
    - This plan consumes the localization and tests added in `04-01`.
    - Release polish is driven by actual failures or inconsistencies found during validation, not by net-new feature work.
---

<objective>
Run the final release hardening loop for the tracker MVP and fix anything real that remains.

Purpose: Convert Phase 4 coverage work into a release-ready tracker by validating the whole MVP and resolving the concrete issues that validation exposes.
Output: Passing validation, targeted final fixes, and updated project state showing the MVP is ready to close the milestone.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-and-release-hardening/04-CONTEXT.md
@.planning/phases/04-quality-and-release-hardening/04-01-PLAN.md
@apps/frontend/app/(default)/dashboard/page.tsx
@apps/frontend/app/(default)/resumes/[id]/page.tsx
@apps/frontend/app/(default)/settings/page.tsx
@apps/frontend/app/(default)/tailor/page.tsx
@apps/backend/app/routers/applications.py
@apps/backend/app/routers/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full tracker hardening validation suite and capture real gaps</name>
  <files>apps/frontend/, apps/backend/</files>
  <read_first>.planning/phases/04-quality-and-release-hardening/04-01-PLAN.md, apps/frontend/package.json, apps/frontend/vitest.config.ts, apps/backend/tests/test_applications_endpoints.py</read_first>
  <action>Run the targeted release validation suite for the tracker MVP: frontend linting, frontend formatting, frontend tests, and the backend tracker tests expanded in Plan 04-01. Use the outputs from those checks to identify concrete release blockers or polish gaps. Do not invent a gap list in advance; the validation results should drive what gets fixed in this plan.</action>
  <verify>test -f .planning/phases/04-quality-and-release-hardening/04-01-SUMMARY.md && echo "04-01 complete"</verify>
  <acceptance_criteria>
    - Validation commands for frontend lint, frontend format, frontend tests, and backend tracker tests are executed
    - Any failures are captured as concrete implementation issues, not vague polish goals
    - If validation passes cleanly, the plan records that explicitly instead of manufacturing extra work
  </acceptance_criteria>
  <done>The hardening pass starts from real validation evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Fix only the real issues surfaced by the validation and final tracker review</name>
  <files>apps/frontend/, apps/backend/</files>
  <read_first>Read the exact failing files or inconsistent tracker surfaces identified in Task 1 before editing them.</read_first>
  <action>Address the concrete issues found during validation and a final tracker-surface review. Typical examples include broken tests, inconsistent locale strings, validation mismatches, or UI polish issues that clearly conflict with the existing Swiss-style system. Keep the fixes tightly scoped to release readiness; do not reopen feature decisions from earlier phases or add new tracker capabilities.</action>
  <verify>Use the same validation commands from Task 1 after applying fixes.</verify>
  <acceptance_criteria>
    - Every code fix maps back to a real failure or inconsistency found in Task 1
    - No new feature work or tracker scope expansion is introduced
    - Resume-centric dashboard, resume viewer, Settings, and tailoring flows remain intact after the fixes
  </acceptance_criteria>
  <done>Only evidence-backed release issues remain addressed.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize release-readiness evidence and milestone-facing project state</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/04-quality-and-release-hardening/04-CONTEXT.md</read_first>
  <action>Once validation passes, update the milestone-facing planning artifacts to reflect the tracker's release-ready state. This includes marking Phase 4 complete, rolling the current state forward, and recording any final project-level decisions or validated outcomes that matter for milestone closure. Keep the updates factual and aligned with the actual validation results.</action>
  <verify>rg -n "Phase 4|QUAL-01|QUAL-02|Complete|release" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - Planning state files reflect Phase 4 completion when validation passes
    - `QUAL-01` and `QUAL-02` are marked complete if and only if the validation evidence supports it
    - No planning file claims release readiness without supporting validation evidence
  </acceptance_criteria>
  <done>The milestone state matches the validated condition of the tracker MVP.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/frontend && npm run lint`
- [ ] `cd apps/frontend && ./node_modules/.bin/prettier --check 'app/(default)/dashboard/page.tsx' 'app/(default)/resumes/[id]/page.tsx' 'app/(default)/settings/page.tsx' 'app/(default)/tailor/page.tsx' 'messages/en.json' 'messages/es.json' 'messages/ja.json' 'messages/pt-BR.json' 'messages/zh.json'`
- [ ] `cd apps/frontend && ./node_modules/.bin/vitest run`
- [ ] `cd apps/backend && python -m unittest apps.backend.tests.test_applications_endpoints`
- [ ] Final fixes preserve the established Swiss-style, resume-centric tracker workflow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The tracker MVP is validated as release-ready for milestone closure
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-and-release-hardening/04-02-SUMMARY.md`
</output>
