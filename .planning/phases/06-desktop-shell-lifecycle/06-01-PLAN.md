---
phase: 06-desktop-shell-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/desktop/tsconfig.json
  - apps/desktop/src/config.ts
  - apps/desktop/src/shared/contracts.ts
  - apps/desktop/src/shared/ipc.ts
  - apps/desktop/src/main.ts
  - apps/desktop/src/preload.ts
autonomous: true
requirements:
  - DSK-01
user_setup: []
must_haves:
  truths:
    - Resume Matcher has a real Electron desktop entrypoint instead of relying on the browser-plus-terminal workflow as the primary shell.
    - The desktop shell has typed lifecycle contracts for startup, ready, failure, retry, and log access before orchestration logic is added.
    - The desktop host is additive around the current Next.js plus FastAPI app instead of replacing either runtime.
  artifacts:
    - path: apps/desktop/package.json
      provides: Electron workspace manifest with build, dev, start, typecheck, and test scripts
      contains: "\"electron\""
    - path: apps/desktop/src/shared/contracts.ts
      provides: Typed desktop lifecycle payloads for preload and renderer-safe status exchange
      contains: "DesktopBootstrapState"
    - path: apps/desktop/src/main.ts
      provides: Electron main-process bootstrap with single-instance protection and one shell window entry
      contains: "requestSingleInstanceLock"
  key_links:
    - from: apps/desktop/src/config.ts
      to: apps/frontend/next.config.ts
      via: localhost origin constants
      pattern: "127.0.0.1:8000|127.0.0.1:3000"
    - from: apps/desktop/src/preload.ts
      to: apps/desktop/src/shared/ipc.ts
      via: contextBridge channel map
      pattern: "desktop-shell:"
---

<objective>
Establish the Electron workspace and lifecycle contracts that Phase 6 will build on.

Purpose: Create a concrete desktop host boundary around the existing app so later plans can add runtime orchestration and window behavior without inventing project structure mid-execution.
Output: A new `apps/desktop` workspace with Electron build scripts, typed lifecycle contracts, a base main-process entry, and a preload bridge skeleton.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md
@docker/start.sh
@Dockerfile
@apps/frontend/package.json
@apps/frontend/next.config.ts
@apps/frontend/lib/api/client.ts
@apps/backend/pyproject.toml

<interfaces>
From `apps/frontend/next.config.ts`:
```ts
const BACKEND_ORIGIN = process.env.BACKEND_ORIGIN || 'http://127.0.0.1:8000';
output: 'standalone';
```

From `apps/frontend/lib/api/client.ts`:
```ts
const DEFAULT_PUBLIC_API_URL = '/';
const INTERNAL_API_ORIGIN = 'http://127.0.0.1:8000';
```

These values are the source of truth for the desktop shell's managed localhost topology.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the desktop workspace manifest and shell contracts</name>
  <files>apps/desktop/package.json, apps/desktop/tsconfig.json, apps/desktop/src/config.ts, apps/desktop/src/shared/contracts.ts, apps/desktop/src/shared/ipc.ts</files>
  <read_first>apps/desktop/package.json, apps/desktop/tsconfig.json, apps/desktop/src/config.ts, apps/desktop/src/shared/contracts.ts, apps/desktop/src/shared/ipc.ts, apps/frontend/package.json, apps/frontend/next.config.ts, apps/frontend/lib/api/client.ts, apps/backend/pyproject.toml, .planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md</read_first>
  <action>Create `apps/desktop/package.json` as a standalone Electron workspace with these exact scripts: `"build": "tsc -p tsconfig.json"`, `"dev": "npm run build && electron dist/main.js"`, `"start": "npm run build && electron dist/main.js"`, `"typecheck": "tsc --noEmit"`, and `"test": "vitest run"`. Add concrete dev dependencies for `electron`, `typescript`, `vitest`, and `@types/node`; do not add installer or updater tooling in this phase. Create `apps/desktop/tsconfig.json` with `rootDir`=`src`, `outDir`=`dist`, `target`=`ES2022`, `module`=`CommonJS`, `moduleResolution`=`Node`, `types` including `node`, and strict type-checking enabled. In `apps/desktop/src/config.ts`, export the exact managed shell constants `DESKTOP_BACKEND_PORT = 8000`, `DESKTOP_FRONTEND_PORT = 3000`, `DESKTOP_BACKEND_ORIGIN = "http://127.0.0.1:8000"`, `DESKTOP_FRONTEND_ORIGIN = "http://127.0.0.1:3000"`, and `DESKTOP_HEALTH_PATH = "/api/v1/health"` so the host matches the existing frontend/backend assumptions. In `apps/desktop/src/shared/contracts.ts`, define the desktop lifecycle types the rest of the phase will use, including `DesktopBootstrapPhase = "starting" | "ready" | "failed" | "stopping"`, `DesktopStartupStep = "booting" | "starting-backend" | "waiting-backend" | "starting-frontend" | "waiting-frontend" | "opening-window"`, and a `DesktopBootstrapState` union that carries `step`, `url`, `message`, `stage`, and `logDirectory` fields. In `apps/desktop/src/shared/ipc.ts`, define the exact IPC channel constants `DESKTOP_GET_STATE = "desktop-shell:get-state"`, `DESKTOP_RETRY = "desktop-shell:retry"`, `DESKTOP_OPEN_LOGS = "desktop-shell:open-logs"`, and `DESKTOP_STATE_EVENT = "desktop-shell:state"`.</action>
  <verify>cd apps/desktop && npm run typecheck</verify>
  <acceptance_criteria>
    - `apps/desktop/package.json` contains the exact script keys `"build"`, `"dev"`, `"start"`, `"typecheck"`, and `"test"`
    - `apps/desktop/package.json` contains the string `"electron"`
    - `apps/desktop/src/config.ts` contains `DESKTOP_BACKEND_PORT = 8000` and `DESKTOP_FRONTEND_PORT = 3000`
    - `apps/desktop/src/shared/contracts.ts` contains `DesktopBootstrapState`
    - `apps/desktop/src/shared/ipc.ts` contains `desktop-shell:get-state`, `desktop-shell:retry`, `desktop-shell:open-logs`, and `desktop-shell:state`
  </acceptance_criteria>
  <done>The repo has a concrete Electron workspace and typed shell contracts that later plans can implement against.</done>
</task>

<task type="auto">
  <name>Task 2: Add the base Electron main-process and preload entrypoints</name>
  <files>apps/desktop/src/main.ts, apps/desktop/src/preload.ts</files>
  <read_first>apps/desktop/src/main.ts, apps/desktop/src/preload.ts, apps/desktop/src/config.ts, apps/desktop/src/shared/contracts.ts, apps/desktop/src/shared/ipc.ts, .planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md, docker/start.sh</read_first>
  <action>Create `apps/desktop/src/main.ts` as the single Electron entrypoint with `app.requestSingleInstanceLock()` guarding multiple launches, `Menu.setApplicationMenu(null)` removing the default app menu, and one base `BrowserWindow` definition sized to `width: 1440`, `height: 960`, `minWidth: 1100`, and `minHeight: 760`. Configure `webPreferences` exactly with `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, and `preload: path.join(__dirname, "preload.js")`. Keep the window hidden initially with `show: false`, set `autoHideMenuBar: true`, and load a minimal placeholder HTML string or data URL that says `Starting Resume Matcher...`; do not add tray behavior, background windows, or installer logic. In `apps/desktop/src/preload.ts`, expose a `desktopShell` bridge through `contextBridge.exposeInMainWorld` with the exact methods `getBootstrapState()`, `retryBootstrap()`, `openLogsDirectory()`, and `onBootstrapState(listener)`. Wire those methods to the channel constants from `apps/desktop/src/shared/ipc.ts`, and keep the preload surface read-only; do not expose raw `ipcRenderer`, `child_process`, or unrestricted shell access.</action>
  <verify>cd apps/desktop && npm run build</verify>
  <acceptance_criteria>
    - `apps/desktop/src/main.ts` contains `requestSingleInstanceLock`
    - `apps/desktop/src/main.ts` contains `Menu.setApplicationMenu(null)`
    - `apps/desktop/src/main.ts` contains `show: false`, `width: 1440`, and `height: 960`
    - `apps/desktop/src/preload.ts` contains `contextBridge.exposeInMainWorld`
    - `apps/desktop/src/preload.ts` exposes the method names `getBootstrapState`, `retryBootstrap`, `openLogsDirectory`, and `onBootstrapState`
  </acceptance_criteria>
  <done>The desktop shell can boot into a controlled Electron window with a safe preload bridge and no extra process model invented outside the current app.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/desktop && npm run typecheck`
- [ ] `cd apps/desktop && npm run build`
- [ ] `rg -n "requestSingleInstanceLock|Menu.setApplicationMenu\\(null\\)|desktop-shell:get-state|DesktopBootstrapState|DESKTOP_BACKEND_PORT = 8000" apps/desktop/src apps/desktop/package.json`
- [ ] `apps/desktop/package.json` does not include `electron-builder`, `electron-forge`, `autoUpdater`, or installer-specific tooling
</verification>

<success_criteria>
The repository contains a dedicated `apps/desktop` Electron shell workspace with explicit lifecycle contracts and a safe single-window main/preload foundation, ready for runtime supervision work.
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-shell-lifecycle/06-01-SUMMARY.md`
</output>
