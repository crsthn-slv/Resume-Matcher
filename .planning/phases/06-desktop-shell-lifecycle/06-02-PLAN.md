---
phase: 06-desktop-shell-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - apps/desktop/src/runtime/command-factory.ts
  - apps/desktop/src/runtime/logging.ts
  - apps/desktop/src/runtime/process-tree.ts
  - apps/desktop/src/runtime/supervisor.ts
  - apps/desktop/tests/runtime-supervisor.test.ts
autonomous: true
requirements:
  - DSK-02
  - DSK-04
user_setup: []
must_haves:
  truths:
    - The desktop host starts backend first, waits for health, then starts frontend without exposing terminal windows to the user.
    - The desktop host owns shutdown for both managed processes and does not leave orphaned backend or frontend processes behind.
    - Desktop startup and shutdown leave actionable log files behind for later retry and debugging flows.
  artifacts:
    - path: apps/desktop/src/runtime/supervisor.ts
      provides: Managed backend/frontend lifecycle controller
      contains: "start()"
    - path: apps/desktop/src/runtime/process-tree.ts
      provides: Windows-aware child teardown helper
      contains: "taskkill"
    - path: apps/desktop/tests/runtime-supervisor.test.ts
      provides: Automated coverage for startup ordering, failure handling, and shutdown
      contains: "backend-before-frontend"
  key_links:
    - from: apps/desktop/src/runtime/supervisor.ts
      to: docker/start.sh
      via: backend-then-frontend startup order
      pattern: "health"
    - from: apps/desktop/src/runtime/command-factory.ts
      to: apps/frontend/next.config.ts
      via: BACKEND_ORIGIN and port contract
      pattern: "127.0.0.1:8000"
---

<objective>
Replace the shell-script runtime orchestration with a hidden Electron-managed supervisor.

Purpose: Preserve the current backend-first lifecycle model from `docker/start.sh`, but move it into Windows-friendly Electron process management that can start, monitor, log, and stop the app cleanly.
Output: A runtime supervisor, command factory, log writer, process-tree cleanup helper, and automated lifecycle tests.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md
@.planning/phases/06-desktop-shell-lifecycle/06-01-PLAN.md
@docker/start.sh
@Dockerfile
@apps/backend/app/main.py
@apps/backend/pyproject.toml
@apps/frontend/next.config.ts
@apps/frontend/lib/api/client.ts
@apps/desktop/src/config.ts
@apps/desktop/src/shared/contracts.ts
@apps/desktop/src/shared/ipc.ts

<interfaces>
Existing runtime truth from `docker/start.sh`:
```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
curl http://127.0.0.1:8000/api/v1/health
node server.js
```

Existing frontend expectations:
```ts
const BACKEND_ORIGIN = process.env.BACKEND_ORIGIN || 'http://127.0.0.1:8000';
const INTERNAL_API_ORIGIN = 'http://127.0.0.1:8000';
const DEFAULT_PUBLIC_API_URL = '/';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the desktop runtime supervisor, command factory, and log sinks</name>
  <files>apps/desktop/src/runtime/command-factory.ts, apps/desktop/src/runtime/logging.ts, apps/desktop/src/runtime/supervisor.ts</files>
  <read_first>apps/desktop/src/runtime/command-factory.ts, apps/desktop/src/runtime/logging.ts, apps/desktop/src/runtime/supervisor.ts, apps/desktop/src/config.ts, apps/desktop/src/shared/contracts.ts, docker/start.sh, Dockerfile, apps/backend/app/main.py, apps/frontend/next.config.ts, apps/frontend/lib/api/client.ts, .planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `apps/desktop/src/runtime/command-factory.ts` contains the exact development backend command `uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level info`
    - `apps/desktop/src/runtime/command-factory.ts` contains the exact development frontend command `npm run dev -- --hostname 127.0.0.1 --port 3000`
    - `apps/desktop/src/runtime/command-factory.ts` contains the exact production frontend command `npm run start -- --hostname 127.0.0.1 --port 3000`
    - `apps/desktop/src/runtime/supervisor.ts` contains `windowsHide: true`
    - `apps/desktop/src/runtime/supervisor.ts` waits on `http://127.0.0.1:8000/api/v1/health` before starting the frontend
    - `apps/desktop/src/runtime/logging.ts` writes `desktop-host.log`, `backend.log`, and `frontend.log`
  </acceptance_criteria>
  <action>Implement `apps/desktop/src/runtime/command-factory.ts` so it returns explicit `command`, `args`, `cwd`, and `env` objects for two exact runtime modes: `development` and `production`. For `development`, use backend command `uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level info` in `apps/backend`, and frontend command `npm run dev -- --hostname 127.0.0.1 --port 3000` in `apps/frontend`. For `production`, use backend command `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level info` in `apps/backend`, and frontend command `npm run start -- --hostname 127.0.0.1 --port 3000` in `apps/frontend`. In both modes, set `BACKEND_ORIGIN=http://127.0.0.1:8000`, `NEXT_PUBLIC_API_URL=/`, and `PYTHONUNBUFFERED=1`; add `NODE_ENV=production` only for the production frontend path. Implement `apps/desktop/src/runtime/logging.ts` to create a desktop log directory under Electron `app.getPath("userData")/logs` and stream host, backend, and frontend stdout/stderr into `desktop-host.log`, `backend.log`, and `frontend.log`. Implement `apps/desktop/src/runtime/supervisor.ts` as the authoritative lifecycle service with `start()` and `stop()` methods, injected spawn/probe dependencies for testing, and phase updates that emit the exact startup steps `starting-backend`, `waiting-backend`, `starting-frontend`, `waiting-frontend`, and `opening-window`. The supervisor must spawn backend first, poll `GET http://127.0.0.1:8000/api/v1/health` up to 30 times with a 1000 ms interval, then spawn frontend and poll `GET http://127.0.0.1:3000/` up to 30 times with a 1000 ms interval. Spawn child processes with `windowsHide: true` and piped stdio so no terminal or console windows appear during normal use.</action>
  <verify>cd apps/desktop && npx vitest run tests/runtime-supervisor.test.ts</verify>
  <done>The desktop shell can start the existing backend and frontend in the right order, on the right ports, with hidden child-process windows and persistent logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Windows-aware process-tree shutdown and supervisor regression tests</name>
  <files>apps/desktop/src/runtime/process-tree.ts, apps/desktop/tests/runtime-supervisor.test.ts</files>
  <read_first>apps/desktop/src/runtime/process-tree.ts, apps/desktop/tests/runtime-supervisor.test.ts, apps/desktop/src/runtime/supervisor.ts, apps/desktop/src/shared/contracts.ts, docker/start.sh, .planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `apps/desktop/src/runtime/process-tree.ts` contains `taskkill`
    - `apps/desktop/src/runtime/process-tree.ts` contains `/pid`, `/t`, and `/f`
    - `apps/desktop/tests/runtime-supervisor.test.ts` contains a test for backend-before-frontend ordering
    - `apps/desktop/tests/runtime-supervisor.test.ts` contains a test for frontend teardown plus backend teardown on stop
    - `apps/desktop/tests/runtime-supervisor.test.ts` contains a test for startup failure when the backend health probe never becomes ready
  </acceptance_criteria>
  <action>Create `apps/desktop/src/runtime/process-tree.ts` as the only process-tree termination helper used by the supervisor. Implement a soft-stop path first, then escalation: on Windows run `taskkill /pid <pid> /t` and escalate to `taskkill /pid <pid> /t /f` after a 5000 ms timeout; on non-Windows platforms send `SIGTERM`, wait 5000 ms, then send `SIGKILL` if the child tree is still alive. Update the supervisor to stop frontend before backend, capture child exit codes, and always execute cleanup when the Electron app is quitting or when either managed process exits unexpectedly. Add `apps/desktop/tests/runtime-supervisor.test.ts` with explicit tests that prove the supervisor never starts frontend before backend health succeeds, fails with a structured `stage` value when the backend probe times out, passes `windowsHide: true` into process creation, and calls the process-tree helper for both frontend and backend during shutdown. Keep tests deterministic by mocking spawn, probe, timers, and process-tree calls; do not shell out to real `uv`, `npm`, or `taskkill` in the test suite.</action>
  <verify>cd apps/desktop && npx vitest run tests/runtime-supervisor.test.ts</verify>
  <done>Managed app processes shut down predictably and Phase 6 has automated evidence that startup and teardown behave correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/desktop && npm run test`
- [ ] `cd apps/desktop && npm run typecheck`
- [ ] `rg -n "uv run uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level info|npm run dev -- --hostname 127.0.0.1 --port 3000|npm run start -- --hostname 127.0.0.1 --port 3000|windowsHide: true|taskkill" apps/desktop/src apps/desktop/tests`
- [ ] `apps/desktop/src/runtime/supervisor.ts` starts backend before frontend and contains the `http://127.0.0.1:8000/api/v1/health` readiness gate
</verification>

<success_criteria>
The desktop shell owns startup, readiness polling, shutdown, and logging for backend and frontend processes without exposing terminals or leaving orphaned tasks behind.
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-shell-lifecycle/06-02-SUMMARY.md`
</output>
