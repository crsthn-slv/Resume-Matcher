---
phase: 06-desktop-shell-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - apps/desktop/src/windows/createSplashWindow.ts
  - apps/desktop/src/windows/createMainWindow.ts
  - apps/desktop/src/windows/createErrorWindow.ts
  - apps/desktop/src/ui/splash.html
  - apps/desktop/src/ui/error.html
  - apps/desktop/src/main.ts
  - apps/desktop/src/preload.ts
  - apps/desktop/tests/window-shell.test.ts
  - apps/desktop/README.md
autonomous: true
requirements:
  - DSK-01
  - DSK-02
  - DSK-04
user_setup: []
must_haves:
  truths:
    - The user sees an intentional splash screen while the managed runtime starts and only sees the main app window after the app is actually ready.
    - If startup fails, the user gets a friendly failure surface with retry and log access instead of a silent exit or raw technical exception.
    - The desktop shell stays single-window, opens external links in the default browser, and exits cleanly when the main window closes.
  artifacts:
    - path: apps/desktop/src/windows/createSplashWindow.ts
      provides: Splash window factory for startup state display
      contains: "Starting Resume Matcher"
    - path: apps/desktop/src/windows/createErrorWindow.ts
      provides: Startup failure UI with retry and open-logs controls
      contains: "Resume Matcher couldn't start"
    - path: apps/desktop/README.md
      provides: Maintainer runbook for desktop shell launch and diagnostics
      contains: "npm run dev"
  key_links:
    - from: apps/desktop/src/main.ts
      to: apps/desktop/src/runtime/supervisor.ts
      via: splash-to-main-to-error state transitions
      pattern: "opening-window|failed"
    - from: apps/desktop/src/main.ts
      to: apps/desktop/src/preload.ts
      via: retry and open-logs IPC
      pattern: "desktop-shell:retry|desktop-shell:open-logs"
---

<objective>
Wire the actual shell lifecycle: splash, main window, failure recovery, external-link policy, and maintainer guidance.

Purpose: Turn the runtime supervisor into a user-facing desktop experience that feels like a normal Windows app instead of a developer toolchain wrapper.
Output: Single-window splash/main/error flow, retryable startup failure screen, external-browser link policy, desktop-shell smoke tests, and a short operator runbook.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md
@.planning/phases/06-desktop-shell-lifecycle/06-02-PLAN.md
@apps/desktop/src/main.ts
@apps/desktop/src/preload.ts
@apps/desktop/src/config.ts
@apps/desktop/src/shared/contracts.ts
@apps/desktop/src/shared/ipc.ts
@apps/desktop/src/runtime/supervisor.ts
@apps/frontend/lib/api/client.ts
@apps/frontend/next.config.ts

<interfaces>
Required lifecycle methods from the preload bridge:
```ts
getBootstrapState()
retryBootstrap()
openLogsDirectory()
onBootstrapState(listener)
```

Required shell policy from context decisions:
- One main window only
- External links open in the system browser
- No tray or minimize-to-tray behavior
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement splash, main, and failure windows with the exact shell lifecycle policy</name>
  <files>apps/desktop/src/windows/createSplashWindow.ts, apps/desktop/src/windows/createMainWindow.ts, apps/desktop/src/windows/createErrorWindow.ts, apps/desktop/src/ui/splash.html, apps/desktop/src/ui/error.html, apps/desktop/src/main.ts, apps/desktop/src/preload.ts</files>
  <read_first>apps/desktop/src/windows/createSplashWindow.ts, apps/desktop/src/windows/createMainWindow.ts, apps/desktop/src/windows/createErrorWindow.ts, apps/desktop/src/ui/splash.html, apps/desktop/src/ui/error.html, apps/desktop/src/main.ts, apps/desktop/src/preload.ts, apps/desktop/src/runtime/supervisor.ts, apps/desktop/src/config.ts, apps/desktop/src/shared/contracts.ts, apps/desktop/src/shared/ipc.ts, apps/frontend/next.config.ts, apps/frontend/lib/api/client.ts, .planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `apps/desktop/src/ui/splash.html` contains `Starting Resume Matcher`
    - `apps/desktop/src/ui/error.html` contains `Resume Matcher couldn't start`, `Try again`, and `Open logs`
    - `apps/desktop/src/main.ts` contains `shell.openExternal`
    - `apps/desktop/src/main.ts` contains `setWindowOpenHandler`
    - `apps/desktop/src/main.ts` contains exactly one main `BrowserWindow` creation path and no tray creation
    - `apps/desktop/src/preload.ts` wires `desktop-shell:retry` and `desktop-shell:open-logs`
  </acceptance_criteria>
  <action>Create three focused window factories. `apps/desktop/src/windows/createSplashWindow.ts` should build a compact splash window sized `640x420`, centered, non-resizable, auto-hidden menu bar, and load `apps/desktop/src/ui/splash.html` with a headline that reads exactly `Starting Resume Matcher`. `apps/desktop/src/windows/createMainWindow.ts` should build the single primary app window sized `1440x960`, minimum `1100x760`, and load `http://127.0.0.1:3000` only after the runtime supervisor reports `phase: "ready"`. `apps/desktop/src/windows/createErrorWindow.ts` should load `apps/desktop/src/ui/error.html` with a title that reads exactly `Resume Matcher couldn't start`, body copy explaining startup failed, a `Try again` button, and an `Open logs` button. Update `apps/desktop/src/main.ts` to show the splash window first, subscribe to supervisor state changes, create the main window only after frontend readiness succeeds, close the splash once the main window emits `ready-to-show`, and replace the splash with the error window when supervisor startup rejects. Keep the shell single-window: do not add tray icons, secondary app windows, or minimize-to-tray logic. Enforce navigation policy in `apps/desktop/src/main.ts` by allowing only `http://127.0.0.1:3000` navigation inside the main window, using `setWindowOpenHandler` plus `will-navigate` guards to deny other targets and forwarding any external `http:` or `https:` links through `shell.openExternal`. Update `apps/desktop/src/preload.ts` so the retry and open-logs methods invoke the exact `desktop-shell:retry` and `desktop-shell:open-logs` channels and the splash/error HTML can listen for `desktop-shell:state` updates without direct Node access.</action>
  <verify>cd apps/desktop && npx vitest run tests/window-shell.test.ts</verify>
  <done>The desktop shell now feels like a controlled Windows app with intentional startup, a friendly failure path, and safe browser navigation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell smoke coverage and a concise desktop runbook</name>
  <files>apps/desktop/tests/window-shell.test.ts, apps/desktop/README.md</files>
  <read_first>apps/desktop/tests/window-shell.test.ts, apps/desktop/README.md, apps/desktop/src/main.ts, apps/desktop/src/preload.ts, apps/desktop/src/runtime/supervisor.ts, apps/desktop/src/shared/ipc.ts, .planning/phases/06-desktop-shell-lifecycle/06-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `apps/desktop/tests/window-shell.test.ts` contains a test covering external-link handling through `shell.openExternal`
    - `apps/desktop/tests/window-shell.test.ts` contains a test covering retry behavior after a failed startup state
    - `apps/desktop/README.md` contains `cd apps/desktop && npm install`
    - `apps/desktop/README.md` contains `npm run dev`, `npm run build`, and `npm run test`
    - `apps/desktop/README.md` contains the log file names `desktop-host.log`, `backend.log`, and `frontend.log`
  </acceptance_criteria>
  <action>Add `apps/desktop/tests/window-shell.test.ts` with targeted coverage for shell policy rather than Electron snapshots. At minimum, verify that foreign `https://` links are denied in-window and forwarded through `shell.openExternal`, that a failed startup state can trigger a second supervisor start through the retry IPC path, and that a ready state causes the splash window teardown plus main-window show flow in the correct order. Keep tests mocked and deterministic; do not spawn real Electron windows in CI. Write `apps/desktop/README.md` as a short maintainer runbook with these exact sections: `Install`, `Run in desktop dev mode`, `Build the shell`, `Run tests`, and `Logs`. Include the exact commands `cd apps/desktop && npm install`, `npm run dev`, `npm run build`, and `npm run test`. Document that the shell expects backend on `127.0.0.1:8000`, frontend on `127.0.0.1:3000`, and stores `desktop-host.log`, `backend.log`, and `frontend.log` under the Electron user-data logs directory for startup retries and support.</action>
  <verify>cd apps/desktop && npm run test</verify>
  <done>The shell lifecycle is covered by smoke-style automated tests and the repo documents how to run and debug the desktop host without drifting into installer or bundling scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/desktop && npm run test`
- [ ] `cd apps/desktop && npm run build`
- [ ] `cd apps/desktop && npm run typecheck`
- [ ] `rg -n "Starting Resume Matcher|Resume Matcher couldn't start|Try again|Open logs|shell.openExternal|setWindowOpenHandler|desktop-shell:retry|desktop-host.log|backend.log|frontend.log" apps/desktop/src apps/desktop/tests apps/desktop/README.md`
- [ ] `apps/desktop/src/main.ts` still creates one main app window and does not introduce tray handling
</verification>

<success_criteria>
The user-facing desktop shell shows a splash while booting, promotes to the real app only when ready, fails with a retryable diagnostic surface, and closes without leaving background processes or browser popups behind.
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-shell-lifecycle/06-03-SUMMARY.md`
</output>
