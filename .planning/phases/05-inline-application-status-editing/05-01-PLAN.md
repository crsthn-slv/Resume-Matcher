---
phase: 05-inline-application-status-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/components/ui/card.tsx
  - apps/frontend/components/ui/dropdown.tsx
  - apps/frontend/lib/api/applications.ts
  - apps/frontend/lib/dashboard/application-filtering.ts
  - apps/frontend/app/(default)/dashboard/page.tsx
  - apps/frontend/messages/en.json
  - apps/frontend/messages/es.json
  - apps/frontend/messages/ja.json
  - apps/frontend/messages/pt-BR.json
  - apps/frontend/messages/zh.json
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Dashboard status badges are the direct interaction trigger for inline status editing instead of passive signaling only.
    - Selecting a status from a dashboard badge saves immediately for linked applications and attempts quick-create for unlinked cards.
    - Failed optimistic updates revert the badge state and surface a lightweight global notification instead of leaving the card in a broken intermediate state.
  artifacts:
    - apps/frontend/components/ui/card.tsx or a nearby shared UI helper exposes an interactive status-badge variant suitable for dashboard cards.
    - apps/frontend/app/(default)/dashboard/page.tsx renders clickable status badges for all tailored-resume cards and wires them to application update/create flows.
    - Locale files contain any new inline-status labels or failure copy introduced by the dashboard interaction.
  key_links:
    - Dashboard inline status updates still use the backend-owned application status endpoint for linked applications.
    - Dashboard quick-create reuses the existing application creation contract and falls back to the normal creation form when inference is insufficient.
---

<objective>
Make dashboard status badges interactive and capable of both inline status updates and quick-create behavior.

Purpose: Turn the dashboard card badge from a passive signal into the fastest way to set or change application status while preserving the existing resume-centric workflow and backend validation rules.
Output: Clickable dashboard badges with anchored dropdowns, optimistic save behavior, quick-create fallback for cards without linked applications, and lightweight global failure feedback.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-inline-application-status-editing/05-CONTEXT.md
@.planning/phases/05-inline-application-status-editing/05-UI-SPEC.md
@apps/frontend/app/(default)/dashboard/page.tsx
@apps/frontend/components/ui/card.tsx
@apps/frontend/components/ui/dropdown.tsx
@apps/frontend/lib/api/applications.ts
@apps/frontend/lib/dashboard/application-filtering.ts
@apps/frontend/app/(default)/resumes/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared inline-status UI primitives and optimistic state helpers</name>
  <files>apps/frontend/components/ui/card.tsx, apps/frontend/components/ui/dropdown.tsx, apps/frontend/lib/api/applications.ts</files>
  <read_first>apps/frontend/components/ui/card.tsx, apps/frontend/components/ui/dropdown.tsx, apps/frontend/lib/api/applications.ts, .planning/phases/05-inline-application-status-editing/05-CONTEXT.md, .planning/phases/05-inline-application-status-editing/05-UI-SPEC.md</read_first>
  <action>Extend the shared UI layer so the existing status badge styling can act as a compact interactive trigger with an in-badge loading state, and extend the dropdown primitive or a nearby helper so it can be rendered as a badge-anchored status menu without large form chrome. In the application API/helper layer, add concrete utility support for the dashboard’s optimistic update flow and quick-create branch: normalize pipeline status options, expose a helper to derive an application create payload from an unlinked tailored-resume card using available resume/job snippet context, and return a clear “insufficient inference” outcome that the dashboard can route into the normal creation form instead of guessing missing `company` or `role` values.</action>
  <verify>rg -n "loading|spinner|interactive|derive|inference|quick-create|insufficient" apps/frontend/components/ui/card.tsx apps/frontend/components/ui/dropdown.tsx apps/frontend/lib/api/applications.ts</verify>
  <acceptance_criteria>
    - `apps/frontend/components/ui/card.tsx` contains interactive status-badge support beyond passive `CardBadge` rendering
    - `apps/frontend/components/ui/card.tsx` or `apps/frontend/components/ui/dropdown.tsx` contains an in-badge loading treatment
    - `apps/frontend/lib/api/applications.ts` contains a helper for dashboard-side quick-create payload derivation or inference result handling
    - The helper layer has an explicit insufficient-inference path rather than silently creating incomplete applications
  </acceptance_criteria>
  <done>The dashboard has shared primitives and helpers for inline badge-triggered status work.</done>
</task>

<task type="auto">
  <name>Task 2: Replace passive dashboard badges with inline status menus and quick-create fallback</name>
  <files>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/messages/en.json, apps/frontend/messages/es.json, apps/frontend/messages/ja.json, apps/frontend/messages/pt-BR.json, apps/frontend/messages/zh.json</files>
  <read_first>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/lib/api/applications.ts, apps/frontend/lib/dashboard/application-filtering.ts, apps/frontend/app/(default)/resumes/[id]/page.tsx, .planning/phases/05-inline-application-status-editing/05-CONTEXT.md, .planning/phases/05-inline-application-status-editing/05-UI-SPEC.md</read_first>
  <action>Update the dashboard card rendering so every tailored-resume card shows a clickable status tag in the existing application-signal area. For cards with a linked application, opening the anchored dropdown and selecting a new status must optimistically switch the badge text/variant immediately, show loading inside the badge, call `updateApplicationStatus`, then either commit the new state or revert on failure. For cards without a linked application, selecting a status must first attempt quick-create by deriving `company`, `role`, `resume_id`, `job_id`, and `job_url` from the existing card and linked job context. If that derivation is insufficient, route into the existing application-creation flow with the maximum available prefill instead of leaving the badge unchanged with no next step. Add any missing locale keys needed for inline status menu copy and the short retryable global failure notification text in all supported message files.</action>
  <verify>rg -n "updateApplicationStatus|createApplication|optimistic|status badge|inline status|saveFailed|quick-create|prefill" 'apps/frontend/app/(default)/dashboard/page.tsx' apps/frontend/messages/*.json</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/dashboard/page.tsx` renders a clickable status badge for cards with and without linked applications
    - Linked cards call `updateApplicationStatus` directly from the badge interaction without a separate confirmation button
    - Unlinked cards attempt quick-create before falling back to the normal creation flow
    - Failed optimistic saves revert the dashboard badge state and surface a user-visible failure message path
    - Any new inline-status strings exist in `apps/frontend/messages/en.json`, `apps/frontend/messages/es.json`, `apps/frontend/messages/ja.json`, `apps/frontend/messages/pt-BR.json`, and `apps/frontend/messages/zh.json`
  </acceptance_criteria>
  <done>The dashboard supports immediate badge-driven status work for both existing and not-yet-created applications.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/frontend && npx eslint app/'(default)'/dashboard/page.tsx components/ui/card.tsx components/ui/dropdown.tsx lib/api/applications.ts lib/dashboard/application-filtering.ts`
- [ ] `cd apps/frontend && npx prettier --check app/'(default)'/dashboard/page.tsx components/ui/card.tsx components/ui/dropdown.tsx lib/api/applications.ts lib/dashboard/application-filtering.ts messages/en.json messages/es.json messages/ja.json messages/pt-BR.json messages/zh.json`
- [ ] `rg -n "updateApplicationStatus|createApplication|optimistic|loading|saveFailed|quick-create|prefill" 'apps/frontend/app/(default)/dashboard/page.tsx' apps/frontend/components/ui/card.tsx apps/frontend/components/ui/dropdown.tsx apps/frontend/lib/api/applications.ts`
- [ ] Dashboard cards keep the existing resume-centric layout and do not introduce a separate tracker table or modal-only status workflow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dashboard badges become the primary status interaction without violating backend status authority or the existing tracker surface
</success_criteria>

<output>
After completion, create `.planning/phases/05-inline-application-status-editing/05-01-SUMMARY.md`
</output>
