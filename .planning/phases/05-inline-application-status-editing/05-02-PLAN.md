---
phase: 05-inline-application-status-editing
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/frontend/app/(default)/resumes/[id]/page.tsx
  - apps/frontend/tests/
  - apps/frontend/messages/en.json
  - apps/frontend/messages/es.json
  - apps/frontend/messages/ja.json
  - apps/frontend/messages/pt-BR.json
  - apps/frontend/messages/zh.json
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - The resume viewer uses the same badge-triggered inline status interaction as the dashboard and no longer requires the separate status-change section plus button.
    - Resume viewer status updates stay optimistic, show loading inside the badge, and revert on failure with global feedback.
    - Frontend regression coverage protects the new inline status flow and the dashboard quick-create/fallback behavior from Phase 5 regressions.
  artifacts:
    - apps/frontend/app/(default)/resumes/[id]/page.tsx removes the old status-control block and wires the main status badge to the inline dropdown flow.
    - Frontend tests exist for at least one dashboard inline-status path and one resume-viewer inline-status path.
    - Locale files cover any new retry/failure or inline-status copy added during the viewer rewrite.
  key_links:
    - Resume viewer inline status updates reuse the same backend status endpoint and configured pipeline options as the dashboard.
    - Phase 5 regression tests assert both optimistic success and failure-revert behavior for inline status changes.
---

<objective>
Bring the resume viewer in line with the new badge-driven status interaction and lock the phase with regression coverage.

Purpose: Finish the inline status editing phase by removing the old button-driven status UI from the detailed application surface and adding targeted frontend tests that protect the shared interaction.
Output: Resume viewer badge-triggered status editing plus regression coverage for the inline interaction and quick-create fallback.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-inline-application-status-editing/05-CONTEXT.md
@.planning/phases/05-inline-application-status-editing/05-UI-SPEC.md
@.planning/phases/05-inline-application-status-editing/05-01-PLAN.md
@apps/frontend/app/(default)/resumes/[id]/page.tsx
@apps/frontend/app/(default)/dashboard/page.tsx
@apps/frontend/lib/api/applications.ts
@apps/frontend/tests/post-tailor-prefill.test.ts
@apps/frontend/tests/dashboard-application-filtering.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the resume viewer status section with the shared inline badge interaction</name>
  <files>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/messages/en.json, apps/frontend/messages/es.json, apps/frontend/messages/ja.json, apps/frontend/messages/pt-BR.json, apps/frontend/messages/zh.json</files>
  <read_first>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/lib/api/applications.ts, apps/frontend/components/ui/dropdown.tsx, .planning/phases/05-inline-application-status-editing/05-CONTEXT.md, .planning/phases/05-inline-application-status-editing/05-UI-SPEC.md</read_first>
  <action>Remove the dedicated status-control panel that currently contains the label, dropdown, and update button inside the resume viewer application area. Replace it with the same badge-triggered inline status interaction pattern introduced for the dashboard: the main status badge in the details grid opens the anchored dropdown, selecting a new status updates optimistically, loading appears inside the badge while saving, and failures revert the badge and trigger the lightweight global notification path. Keep the rest of the application panel — company, role, linked resume, notes, job URL, job description, and status history — intact. Add or update locale strings only where the inline interaction introduces genuinely new copy or error text.</action>
  <verify>rg -n "statusControlLabel|statusSelectLabel|updateStatusAction|updateApplicationStatus|loading|saveFailed|badge" 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/messages/*.json</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/resumes/[id]/page.tsx` no longer renders the old status-change block with a standalone update button
    - The main status badge in the application details area acts as the inline status-edit trigger
    - Resume viewer status changes still call `updateApplicationStatus`
    - Failed resume-viewer status updates revert the optimistic badge state and trigger a user-visible failure feedback path
  </acceptance_criteria>
  <done>The resume viewer matches the new inline status interaction and removes the old button-driven status UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend regression coverage for dashboard and resume viewer inline status flows</name>
  <files>apps/frontend/tests/</files>
  <read_first>apps/frontend/tests/dashboard-application-filtering.test.ts, apps/frontend/tests/post-tailor-prefill.test.ts, apps/frontend/vitest.config.ts, apps/frontend/vitest.setup.ts, apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/lib/api/applications.ts</read_first>
  <action>Add targeted Vitest coverage for the new inline status phase. At minimum, cover one dashboard path where the badge interaction updates or attempts to create an application, one dashboard path where insufficient inference routes into the fallback creation flow, and one resume viewer path where the badge interaction updates status optimistically and reverts on failure. Keep tests focused on behavior and state transitions rather than large snapshots, and reuse the repo’s current testing and mocking conventions instead of introducing a new harness.</action>
  <verify>rg -n "inline status|quick-create|optimistic|revert|resume viewer|dashboard" apps/frontend/tests</verify>
  <acceptance_criteria>
    - `apps/frontend/tests/` contains Phase 5-specific inline status regression coverage
    - At least one test covers a dashboard inline-status success or create path
    - At least one test covers a dashboard insufficient-inference fallback path
    - At least one test covers a resume-viewer inline-status optimistic update and failure revert path
  </acceptance_criteria>
  <done>Phase 5 behavior is protected by focused frontend regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/frontend && npx eslint app/'(default)'/resumes/'[id]'/page.tsx tests`
- [ ] `cd apps/frontend && npx vitest run`
- [ ] `cd apps/frontend && npx prettier --check app/'(default)'/resumes/'[id]'/page.tsx tests messages/en.json messages/es.json messages/ja.json messages/pt-BR.json messages/zh.json`
- [ ] `rg -n "updateApplicationStatus|optimistic|revert|inline status|quick-create|fallback" 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/tests`
- [ ] Resume viewer preserves the current application details/history surface while removing the old standalone status-change block
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 ends with one consistent badge-driven status interaction across dashboard and resume viewer, backed by regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-inline-application-status-editing/05-02-SUMMARY.md`
</output>
