---
phase: 08-installer-release-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - package.json
  - .github/workflows/windows-desktop-release.yml
  - docs/release/windows-desktop-release.md
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "The main repository contains a canonical Windows release workflow that runs on GitHub Actions per D-03 and D-04."
    - "A maintainer can trigger the same release build contract from CI and from a local Windows fallback command without inventing extra steps."
    - "Versioning, artifact naming, and output locations are documented so a new Windows release is repeatable."
  artifacts:
    - path: ".github/workflows/windows-desktop-release.yml"
      provides: "Canonical Windows release pipeline"
    - path: "package.json"
      provides: "Root maintainer entrypoints for Windows release packaging"
    - path: "docs/release/windows-desktop-release.md"
      provides: "Versioning and packaging runbook for maintainers"
  key_links:
    - from: ".github/workflows/windows-desktop-release.yml"
      to: "package.json"
      via: "workflow step invoking root release script"
      pattern: "desktop:release:win"
    - from: "package.json"
      to: "apps/desktop/package.json"
      via: "script delegation into desktop release scripts"
      pattern: "npm --prefix apps/desktop run release:win"
---

<objective>
Create the canonical Windows release automation path in the main repository and document the versioning and packaging flow for maintainers.

Purpose: Satisfy the release-operations requirements with a single, repeatable CI path while preserving a deliberate local fallback instead of a separate distribution fork.
Output: Root release scripts, a Windows GitHub Actions workflow, and maintainer release documentation.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-installer-release-delivery/08-CONTEXT.md
@.planning/phases/07-bundled-runtime-persistent-data/07-01-SUMMARY.md
@.planning/phases/08-installer-release-delivery/08-01-PLAN.md
@package.json
@apps/desktop/package.json
@.github/workflows/docker-publish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical GitHub Actions Windows release pipeline</name>
  <read_first>.planning/phases/08-installer-release-delivery/08-CONTEXT.md, package.json, apps/desktop/package.json, .github/workflows/docker-publish.yml, .planning/phases/08-installer-release-delivery/08-01-PLAN.md</read_first>
  <files>package.json, .github/workflows/windows-desktop-release.yml</files>
  <acceptance_criteria>
    - `package.json` contains scripts `desktop:release:win` and `desktop:release:win:dir`
    - `.github/workflows/windows-desktop-release.yml` runs on `workflow_dispatch` and semver tags matching `v*.*.*`
    - The workflow uses `windows-latest`
    - The workflow installs Node, Python, and `uv`, then runs the root script `npm run desktop:release:win`
    - The workflow uploads installer artifacts from `dist/desktop-installer`
    - The workflow does not add auto-update feed publish steps or external release infrastructure
  </acceptance_criteria>
  <action>
    Add root scripts in `package.json` per D-03 and D-04:
    - `desktop:release:win`: `npm --prefix apps/desktop run release:win`
    - `desktop:release:win:dir`: `npm --prefix apps/desktop run pack:win`

    Create `.github/workflows/windows-desktop-release.yml` as the canonical Windows pipeline. The workflow should:
    - trigger on `workflow_dispatch` and git tags `v*.*.*`
    - run on `windows-latest`
    - check out the repository
    - install Node 22 with npm cache
    - install Python 3.13
    - install `uv`
    - run `npm ci` in `apps/frontend` and `apps/desktop`
    - run `npm run desktop:release:win`
    - upload `dist/desktop-installer/**`

    Keep the workflow focused on build-and-upload only. Do not add code signing, hosted update feeds, GitHub Release publishing, or parallel Linux/macOS matrix jobs in this phase.
  </action>
  <verify>
    <automated>rg -n "desktop:release:win|desktop:release:win:dir" package.json && rg -n "windows-latest|workflow_dispatch|v\\*\\.\\*\\.\\*|dist/desktop-installer|npm run desktop:release:win" .github/workflows/windows-desktop-release.yml</automated>
  </verify>
  <done>The repo exposes one canonical Windows CI build flow and one local fallback command that both target the same installer packaging contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document versioning and packaging flow for maintainers</name>
  <read_first>.planning/phases/08-installer-release-delivery/08-CONTEXT.md, .github/workflows/windows-desktop-release.yml, package.json, apps/desktop/package.json, .github/workflows/docker-publish.yml</read_first>
  <files>docs/release/windows-desktop-release.md</files>
  <acceptance_criteria>
    - `docs/release/windows-desktop-release.md` includes sections named `Version source`, `Canonical CI release`, `Local fallback`, and `Artifacts`
    - The document states the release version comes from `apps/desktop/package.json`
    - The document includes the command `npm run desktop:release:win`
    - The document includes the artifact path `dist/desktop-installer`
    - The document states that GitHub Actions is the official release path and local Windows execution is fallback only
  </acceptance_criteria>
  <action>
    Write `docs/release/windows-desktop-release.md` with concrete maintainer guidance:
    - `Version source`: `apps/desktop/package.json` version must be bumped before tagging
    - `Canonical CI release`: push a semver tag like `v1.1.0`, let `windows-desktop-release.yml` build on GitHub Actions, and collect the `.exe` from `dist/desktop-installer` artifact upload
    - `Local fallback`: run `npm run desktop:release:win` on a prepared Windows machine only when CI is unavailable
    - `Artifacts`: installer name pattern `ResumeMatcher-Setup-${version}.exe` and unpacked directory build from `desktop:release:win:dir`

    Explicitly document that this flow stays in the main repository and that v1.1 does not add auto-update or a separate distribution fork.
  </action>
  <verify>
    <automated>rg -n "Version source|Canonical CI release|Local fallback|Artifacts|apps/desktop/package.json|npm run desktop:release:win|dist/desktop-installer" docs/release/windows-desktop-release.md</automated>
  </verify>
  <done>Maintainers have a concrete release runbook tied to the actual scripts and workflow committed in the repository.</done>
</task>

</tasks>

<verification>
Check the workflow file for the exact Windows runner, trigger, and artifact path strings. Confirm the maintainer doc references the same root release scripts and output directory that CI uses.
</verification>

<success_criteria>
The main repository contains a documented and repeatable Windows release flow where GitHub Actions is official, local Windows release is fallback, and versioning plus artifact handling are explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-release-delivery/08-02-SUMMARY.md`
</output>
