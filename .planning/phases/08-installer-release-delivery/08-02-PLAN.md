---
phase: 08-installer-release-delivery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/windows-desktop-release.yml
  - scripts/desktop/build-runtime.mjs
  - package.json
  - apps/desktop/package.json
  - docs/releases/windows-desktop.md
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - The official Windows release can be generated reproducibly from GitHub Actions instead of depending on one developer machine.
    - The CI pipeline builds the runtime bundle first, then packages the installer from the same repository state.
    - Maintainers have a documented release flow for generating a new Windows installer.
  artifacts:
    - path: .github/workflows/windows-desktop-release.yml
      provides: Canonical GitHub Actions workflow for Windows installer builds
      contains: "windows-latest"
    - path: docs/releases/windows-desktop.md
      provides: Human-readable release steps for tagging, building, and collecting installer artifacts
      contains: "GitHub Actions"
    - path: package.json
      provides: Root scripts used by CI for runtime and packaging orchestration
      contains: "desktop:bundle-runtime"
  key_links:
    - from: .github/workflows/windows-desktop-release.yml
      to: scripts/desktop/build-runtime.mjs
      via: runtime bundle built before installer packaging
      pattern: "desktop:bundle-runtime"
    - from: .github/workflows/windows-desktop-release.yml
      to: apps/desktop/package.json
      via: installer package job invoking package:win
      pattern: "package:win"
---

<objective>
Create the canonical GitHub Actions release pipeline and maintainer runbook for official Windows builds.

Purpose: Phase 8 requires a repeatable release process, not just local packaging commands. This plan makes CI the official path for building installer artifacts from the main repository.
Output: Windows release workflow, CI-consumable build commands, and release documentation for maintainers.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-installer-release-delivery/08-CONTEXT.md
@package.json
@apps/desktop/package.json
@scripts/desktop/build-runtime.mjs
@.github/workflows/docker-publish.yml

<interfaces>
The official release flow must be GitHub Actions on Windows, and must package the installer from the same repo without a separate fork.

The CI sequence should be explicit:
1. checkout repo
2. install dependencies
3. build runtime bundle
4. package Windows installer
5. upload artifacts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the official Windows desktop release workflow in GitHub Actions</name>
  <files>.github/workflows/windows-desktop-release.yml, package.json, apps/desktop/package.json</files>
  <read_first>.github/workflows/docker-publish.yml, package.json, apps/desktop/package.json, scripts/desktop/build-runtime.mjs, .planning/phases/08-installer-release-delivery/08-CONTEXT.md</read_first>
  <action>Create `.github/workflows/windows-desktop-release.yml` as the canonical Windows release workflow. It must run on `windows-latest`, trigger on `workflow_dispatch` and version tags, set up Node and Python, install desktop/frontend/backend dependencies, run the exact root command `npm run desktop:bundle-runtime`, then run `cd apps/desktop && npm run package:win`. Upload the resulting installer artifacts from `apps/desktop/dist/desktop-installer` or the configured builder output directory using `actions/upload-artifact`. Update `package.json` or `apps/desktop/package.json` only as needed so the workflow can call stable script names instead of hard-coded one-off shell commands. The workflow must not publish auto-update metadata or sign releases in this phase; it only needs to build and preserve installer artifacts deterministically.</action>
  <verify>rg -n "windows-latest|workflow_dispatch|desktop:bundle-runtime|package:win|upload-artifact" .github/workflows/windows-desktop-release.yml package.json apps/desktop/package.json</verify>
  <acceptance_criteria>
    - `.github/workflows/windows-desktop-release.yml` contains `windows-latest`
    - `.github/workflows/windows-desktop-release.yml` contains `workflow_dispatch`
    - `.github/workflows/windows-desktop-release.yml` contains `desktop:bundle-runtime`
    - `.github/workflows/windows-desktop-release.yml` contains `package:win`
    - `.github/workflows/windows-desktop-release.yml` contains `upload-artifact`
  </acceptance_criteria>
  <done>The repository now has one canonical CI workflow for building Windows installer artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Document the maintainer release flow around the GitHub Actions build</name>
  <files>docs/releases/windows-desktop.md, apps/desktop/README.md</files>
  <read_first>docs/releases, apps/desktop/README.md, .github/workflows/windows-desktop-release.yml, .planning/phases/08-installer-release-delivery/08-CONTEXT.md</read_first>
  <action>Create `docs/releases/windows-desktop.md` with the exact sections `Purpose`, `Release prerequisites`, `Triggering the GitHub Actions build`, `Collected artifacts`, and `Manual fallback`. Document the canonical release sequence using GitHub Actions, including tag-or-dispatch invocation, expected installer artifact location, and the fact that the official Windows release comes from CI. Update `apps/desktop/README.md` with a short link or section pointing maintainers to `docs/releases/windows-desktop.md` for official release instructions. Keep the fallback path limited to a documented local Windows build for emergencies; do not promote it as the default flow.</action>
  <verify>rg -n "Purpose|Release prerequisites|Triggering the GitHub Actions build|Collected artifacts|Manual fallback|GitHub Actions" docs/releases/windows-desktop.md apps/desktop/README.md</verify>
  <acceptance_criteria>
    - `docs/releases/windows-desktop.md` contains `Purpose`
    - `docs/releases/windows-desktop.md` contains `Triggering the GitHub Actions build`
    - `docs/releases/windows-desktop.md` contains `Collected artifacts`
    - `docs/releases/windows-desktop.md` contains `Manual fallback`
    - `apps/desktop/README.md` references `docs/releases/windows-desktop.md`
  </acceptance_criteria>
  <done>The official release path is documented and maintainers know how to trigger and collect Windows installer builds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "windows-latest|workflow_dispatch|desktop:bundle-runtime|package:win|upload-artifact" .github/workflows/windows-desktop-release.yml`
- [ ] `rg -n "Triggering the GitHub Actions build|Collected artifacts|Manual fallback" docs/releases/windows-desktop.md`
- [ ] `apps/desktop/README.md` points to the Windows release runbook
</verification>

<success_criteria>
The main repository has a repeatable Windows installer release pipeline in GitHub Actions plus maintainer-facing documentation for using it.
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-release-delivery/08-02-SUMMARY.md`
</output>
