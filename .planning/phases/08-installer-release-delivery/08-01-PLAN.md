---
phase: 08-installer-release-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/desktop/package-lock.json
  - apps/desktop/electron-builder.json
  - apps/desktop/tests/packaging-config.test.ts
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
must_haves:
  truths:
    - "A maintainer can generate one Windows NSIS installer `.exe` from the Electron workspace instead of exposing internal runtime executables."
    - "The installer defaults to machine-wide installation and creates Desktop plus Start Menu entry points."
    - "The installed app still resolves the Phase 7 packaged runtime from `process.resourcesPath/dist/desktop` without requiring Python, Node.js, uv, or npm on the target machine."
  artifacts:
    - path: "apps/desktop/electron-builder.json"
      provides: "Canonical electron-builder and NSIS contract for Windows packaging"
    - path: "apps/desktop/package.json"
      provides: "Desktop release scripts that build the shell, bundle the runtime, and invoke electron-builder"
    - path: "apps/desktop/tests/packaging-config.test.ts"
      provides: "Regression coverage for installer config and packaged runtime resource paths"
  key_links:
    - from: "apps/desktop/electron-builder.json"
      to: "apps/desktop/src/main.ts"
      via: "extraResources target `dist/desktop` under Electron resources"
      pattern: "dist/desktop"
    - from: "apps/desktop/package.json"
      to: "scripts/desktop/build-runtime.mjs"
      via: "bundle-runtime script before installer packaging"
      pattern: "bundle:runtime"
---

<objective>
Create the Windows installer packaging contract that turns the Phase 7 runtime bundle into one normal `Resume Matcher` installer `.exe`.

Purpose: Lock the installer implementation first so later CI and documentation work reference a real NSIS packaging surface instead of placeholders.
Output: Electron builder config, desktop release scripts, and packaging regression coverage for the Windows installer path.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-installer-release-delivery/08-CONTEXT.md
@.planning/phases/07-bundled-runtime-persistent-data/07-01-SUMMARY.md
@.planning/phases/07-bundled-runtime-persistent-data/07-03-SUMMARY.md
@apps/desktop/package.json
@apps/desktop/src/main.ts
@apps/desktop/src/runtime/packaged-layout.ts
@scripts/desktop/build-runtime.mjs

<interfaces>
From apps/desktop/src/main.ts:
```typescript
const packagedRuntimeRoot = app.isPackaged
  ? path.join(process.resourcesPath, 'dist/desktop')
  : undefined;
```

From apps/desktop/src/runtime/packaged-layout.ts:
```typescript
export function resolvePackagedRuntimeLayout(baseDir: string): PackagedRuntimeLayout {
  return {
    frontendServer: path.join(baseDir, 'runtime/frontend/server.js'),
    backendExecutable: path.join(baseDir, 'runtime/backend/rm-backend/rm-backend.exe'),
    nodeExecutable: path.join(baseDir, 'runtime/bin/node.exe'),
    playwrightBrowsersDir: path.join(baseDir, 'runtime/playwright'),
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the electron-builder and NSIS packaging contract</name>
  <read_first>.planning/phases/08-installer-release-delivery/08-CONTEXT.md, apps/desktop/package.json, apps/desktop/src/main.ts, apps/desktop/src/runtime/packaged-layout.ts, scripts/desktop/build-runtime.mjs</read_first>
  <files>apps/desktop/package.json, apps/desktop/package-lock.json, apps/desktop/electron-builder.json</files>
  <acceptance_criteria>
    - `apps/desktop/package.json` contains scripts named `pack:win`, `dist:win`, and `release:win`
    - `apps/desktop/package.json` adds `electron-builder` as a desktop dev dependency
    - `apps/desktop/electron-builder.json` contains `"productName": "Resume Matcher"`
    - `apps/desktop/electron-builder.json` contains `"artifactName": "ResumeMatcher-Setup-${version}.${ext}"`
    - `apps/desktop/electron-builder.json` contains `"target": "nsis"`
    - `apps/desktop/electron-builder.json` contains `"perMachine": true`
    - `apps/desktop/electron-builder.json` contains `"createDesktopShortcut": "always"`
    - `apps/desktop/electron-builder.json` contains `"createStartMenuShortcut": true`
    - `apps/desktop/electron-builder.json` contains an `extraResources` mapping from `../../dist/desktop` to `dist/desktop`
  </acceptance_criteria>
  <behavior>
    - Test 1: the packaging config points to one NSIS `.exe` artifact, not portable multi-executable output
    - Test 2: the packaging config preserves the Phase 7 runtime path under `process.resourcesPath/dist/desktop`
    - Test 3: the installer defaults target machine-wide installation and standard Windows shortcuts
  </behavior>
  <action>
    Update `apps/desktop/package.json` per D-01, D-02, D-05, and D-06 to add `electron-builder` plus Windows packaging scripts:
    - `pack:win`: `electron-builder --win nsis --dir --config electron-builder.json`
    - `dist:win`: `electron-builder --win nsis --config electron-builder.json`
    - `release:win`: `npm run build && npm run bundle:runtime && npm run dist:win`

    Create `apps/desktop/electron-builder.json` with these concrete values:
    - `appId`: `com.resume.matcher.desktop`
    - `productName`: `Resume Matcher`
    - `asar`: `true`
    - `directories.output`: `../../dist/desktop-installer`
    - `files`: include desktop compiled output and workspace `package.json`
    - `extraResources`: copy `../../dist/desktop` to `dist/desktop`
    - `win.target`: NSIS for `x64`
    - `artifactName`: `ResumeMatcher-Setup-${version}.${ext}`
    - `nsis.oneClick`: `false`
    - `nsis.perMachine`: `true`
    - `nsis.allowElevation`: `true`
    - `nsis.allowToChangeInstallationDirectory`: `false`
    - `nsis.createDesktopShortcut`: `"always"`
    - `nsis.createStartMenuShortcut`: `true`
    - `nsis.shortcutName`: `Resume Matcher`
    - `nsis.deleteAppDataOnUninstall`: `false`

    Do not introduce `electron-updater`, Squirrel, or auto-update feed settings in this phase. Keep the user-facing delivery as one normal installer `.exe`, not a portable folder or multiple visible runtime executables.
  </action>
  <verify>
    <automated>cd apps/desktop && npm run typecheck</automated>
  </verify>
  <done>The desktop workspace has a concrete NSIS installer contract and a maintainer can invoke one script to produce a machine-wide Windows installer from the packaged runtime bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for the packaging contract</name>
  <read_first>apps/desktop/electron-builder.json, apps/desktop/package.json, apps/desktop/tests/runtime-supervisor.test.ts, apps/desktop/src/main.ts, apps/desktop/src/runtime/packaged-layout.ts</read_first>
  <files>apps/desktop/tests/packaging-config.test.ts</files>
  <acceptance_criteria>
    - `apps/desktop/tests/packaging-config.test.ts` imports the builder config and asserts `productName === "Resume Matcher"`
    - The test file asserts the Windows target is NSIS and `nsis.perMachine === true`
    - The test file asserts `extraResources` contains a `from` path ending in `../../dist/desktop` and a `to` path equal to `dist/desktop`
    - The test file asserts the artifact naming pattern is `ResumeMatcher-Setup-${version}.${ext}`
  </acceptance_criteria>
  <behavior>
    - Test 1: packaging config keeps the packaged runtime under the path expected by `main.ts`
    - Test 2: packaging config keeps Program Files and shortcut defaults locked for Windows installs
    - Test 3: artifact naming stays stable for release documentation and CI artifact publishing
  </behavior>
  <action>
    Add `apps/desktop/tests/packaging-config.test.ts` using Vitest. Import the JSON builder config and assert these exact values:
    - `productName` is `Resume Matcher`
    - `artifactName` is `ResumeMatcher-Setup-${version}.${ext}`
    - `win.target` includes `{ target: "nsis", arch: ["x64"] }`
    - `nsis.perMachine` is `true`
    - `nsis.createDesktopShortcut` is `"always"`
    - `nsis.createStartMenuShortcut` is `true`
    - `extraResources` includes `{ from: "../../dist/desktop", to: "dist/desktop" }`

    Follow the existing desktop test style from `apps/desktop/tests/runtime-supervisor.test.ts`: focused assertions, no snapshot files, and no real packaging run inside the unit test suite.
  </action>
  <verify>
    <automated>cd apps/desktop && npm run test -- packaging-config.test.ts</automated>
  </verify>
  <done>The installer configuration is covered by an automated regression test that fails if the NSIS target, Program Files install mode, shortcuts, artifact naming, or packaged runtime resource path drift.</done>
</task>

</tasks>

<verification>
Run the desktop unit suite for the new packaging test and the desktop typecheck. Confirm the builder config preserves `process.resourcesPath/dist/desktop` so no packaged-runtime main-process change is required.
</verification>

<success_criteria>
`apps/desktop` contains a repeatable NSIS installer config and release scripts that bundle the runtime first, produce one installer `.exe`, and preserve machine-wide installation plus Desktop and Start Menu entry points.
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-release-delivery/08-01-SUMMARY.md`
</output>
