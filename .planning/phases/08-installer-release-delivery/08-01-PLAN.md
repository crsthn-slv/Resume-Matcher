---
phase: 08-installer-release-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/desktop/electron-builder.json
  - apps/desktop/scripts/prepareInstallerPayload.mjs
  - apps/desktop/scripts/assertWindowsBundle.mjs
  - apps/desktop/README.md
autonomous: true
requirements:
  - INST-01
  - INST-03
  - REL-01
must_haves:
  truths:
    - The desktop workspace can package the existing Electron shell and Phase 7 runtime bundle into one installer-oriented Windows app layout.
    - The installer pipeline consumes the packaged runtime bundle instead of expecting users to run internal executables directly.
    - The release packaging still comes from the main repository instead of a separate distribution fork.
  artifacts:
    - path: apps/desktop/electron-builder.json
      provides: Canonical Electron Builder and NSIS packaging configuration
      contains: "\"nsis\""
    - path: apps/desktop/scripts/prepareInstallerPayload.mjs
      provides: Pre-packaging script that stages the Phase 7 runtime bundle for electron-builder
      contains: "dist/desktop/runtime"
    - path: apps/desktop/scripts/assertWindowsBundle.mjs
      provides: Validation of required Windows installer payload contents before packaging
      contains: "rm-backend.exe"
  key_links:
    - from: apps/desktop/scripts/prepareInstallerPayload.mjs
      to: scripts/desktop/build-runtime.mjs
      via: installer payload built from the existing runtime bundle
      pattern: "dist/desktop/runtime"
    - from: apps/desktop/electron-builder.json
      to: apps/desktop/src/runtime/packaged-layout.ts
      via: packaged runtime included in final app resources
      pattern: "runtime"
---

<objective>
Add the Electron Builder + NSIS packaging layer that turns the Phase 7 runtime bundle into a single Windows installer-oriented app payload.

Purpose: Phase 8 starts by collapsing the internal runtime artifacts into one normal Windows delivery path so the end user never has to choose between multiple executables.
Output: Electron Builder config, installer payload staging scripts, and workspace package scripts that build a single NSIS installer target from the repo.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-installer-release-delivery/08-CONTEXT.md
@.planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md
@apps/desktop/package.json
@package.json
@apps/desktop/src/main.ts
@apps/desktop/src/runtime/packaged-layout.ts
@scripts/desktop/build-runtime.mjs

<interfaces>
The installer must consume the existing bundled runtime tree from Phase 7:
```text
dist/desktop/runtime/frontend
dist/desktop/runtime/backend/rm-backend/rm-backend.exe
dist/desktop/runtime/bin/node.exe
dist/desktop/runtime/playwright
```

Phase 8 must wrap that into one NSIS installer `.exe`, not expose those internals to the user.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add electron-builder NSIS configuration and maintainer packaging scripts</name>
  <files>apps/desktop/package.json, apps/desktop/electron-builder.json</files>
  <read_first>apps/desktop/package.json, apps/desktop/src/main.ts, apps/desktop/src/runtime/packaged-layout.ts, .planning/phases/08-installer-release-delivery/08-CONTEXT.md</read_first>
  <action>Update `apps/desktop/package.json` to add the exact dev dependencies `electron-builder` and any builder typing/runtime helpers actually required for packaging, without removing the existing Electron shell scripts. Add installer scripts named exactly `package:win`, `package:win:dir`, and `package:win:nsis`. `package:win` must run the TypeScript build, stage the installer payload, and invoke `electron-builder --config electron-builder.json --win nsis`. `package:win:dir` must invoke the unpacked directory target for local inspection, and `package:win:nsis` must be an explicit alias for the NSIS installer build. Create `apps/desktop/electron-builder.json` as the canonical packaging config with these concrete decisions: `appId` set to a stable reverse-DNS identifier for Resume Matcher, `productName` set to `Resume Matcher`, `directories.output` pointing at `dist/desktop-installer`, `directories.buildResources` pointing at `build`, `files` including the Electron app output under `dist/**`, `extraResources` including the staged runtime payload, `asar` enabled, and a `win` target configured for `nsis`. The `nsis` block must configure a one-click installer disabled, per-machine install enabled, allow elevation enabled, installation path under `Program Files`, and shortcut creation for Desktop and Start Menu. Do not add auto-update providers in this phase.</action>
  <verify>cd apps/desktop && npm run package:win:dir -- --config electron-builder.json</verify>
  <acceptance_criteria>
    - `apps/desktop/package.json` contains `electron-builder`
    - `apps/desktop/package.json` contains `package:win`, `package:win:dir`, and `package:win:nsis`
    - `apps/desktop/electron-builder.json` contains `"nsis"`
    - `apps/desktop/electron-builder.json` contains `"perMachine": true`
    - `apps/desktop/electron-builder.json` contains Desktop and Start Menu shortcut settings
    - `apps/desktop/electron-builder.json` does not contain an auto-update publish provider
  </acceptance_criteria>
  <done>The desktop workspace has a concrete Electron Builder + NSIS configuration that targets a single Windows installer flow.</done>
</task>

<task type="auto">
  <name>Task 2: Stage and validate the installer payload from the Phase 7 runtime bundle</name>
  <files>apps/desktop/scripts/prepareInstallerPayload.mjs, apps/desktop/scripts/assertWindowsBundle.mjs, apps/desktop/README.md</files>
  <read_first>scripts/desktop/build-runtime.mjs, apps/desktop/src/runtime/packaged-layout.ts, apps/desktop/package.json, .planning/phases/08-installer-release-delivery/08-CONTEXT.md</read_first>
  <action>Create `apps/desktop/scripts/prepareInstallerPayload.mjs` to copy the already-built runtime bundle from `dist/desktop/runtime` into an Electron-builder staging directory under `apps/desktop/dist/resources/runtime`, preserving the exact runtime tree expected by `packaged-layout.ts`. The script must fail fast if `dist/desktop/runtime/frontend/server.js`, `dist/desktop/runtime/backend/rm-backend/rm-backend.exe`, `dist/desktop/runtime/bin/node.exe`, or the Playwright directory are missing. Create `apps/desktop/scripts/assertWindowsBundle.mjs` as a reusable verifier that checks those exact required files before packaging and prints a concise pass/fail summary. Update `apps/desktop/README.md` with a short installer-packaging section documenting that maintainers must first run the runtime build and then the installer packaging command, using the exact commands `npm run desktop:bundle-runtime` from repo root and `cd apps/desktop && npm run package:win` for the Windows package step. Do not document the internal executables as user-facing entrypoints.</action>
  <verify>node apps/desktop/scripts/assertWindowsBundle.mjs</verify>
  <acceptance_criteria>
    - `apps/desktop/scripts/prepareInstallerPayload.mjs` contains `dist/desktop/runtime`
    - `apps/desktop/scripts/prepareInstallerPayload.mjs` contains `server.js`, `rm-backend.exe`, and `node.exe`
    - `apps/desktop/scripts/assertWindowsBundle.mjs` contains `rm-backend.exe`
    - `apps/desktop/scripts/assertWindowsBundle.mjs` contains `runtime/playwright` or `playwright`
    - `apps/desktop/README.md` contains `npm run desktop:bundle-runtime`
    - `apps/desktop/README.md` contains `npm run package:win`
  </acceptance_criteria>
  <done>The installer build now consumes a validated runtime payload instead of assuming internal artifacts exist implicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node apps/desktop/scripts/assertWindowsBundle.mjs`
- [ ] `cd apps/desktop && npm run package:win:dir -- --config electron-builder.json`
- [ ] `rg -n "package:win|electron-builder|nsis|perMachine|dist/desktop/runtime|rm-backend.exe|node.exe" apps/desktop/package.json apps/desktop/electron-builder.json apps/desktop/scripts apps/desktop/README.md`
</verification>

<success_criteria>
The repo has a concrete Electron Builder + NSIS packaging layer that can stage the Phase 7 runtime bundle into a single Windows installer-oriented app payload.
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-release-delivery/08-01-SUMMARY.md`
</output>
