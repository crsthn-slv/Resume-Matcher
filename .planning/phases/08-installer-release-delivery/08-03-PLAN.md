---
phase: 08-installer-release-delivery
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - docs/release/windows-install-upgrade.md
  - apps/desktop/README.md
  - EASY-SETUP.md
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "The user-facing Windows install and upgrade path is documented and explicitly preserves Phase 7 local data."
    - "The upgrade path uses a newer installer over the existing installation per D-07, D-08, and D-09 instead of side-by-side installs or auto-update infrastructure."
    - "Maintainers and end users know where the installed app lives and where user data lives, so reinstalling does not imply data loss."
  artifacts:
    - path: "docs/release/windows-install-upgrade.md"
      provides: "End-user and maintainer install/upgrade guidance"
    - path: "apps/desktop/README.md"
      provides: "Desktop workspace instructions aligned to installer packaging"
    - path: "EASY-SETUP.md"
      provides: "High-level install guidance for a non-technical Windows user"
  key_links:
    - from: "docs/release/windows-install-upgrade.md"
      to: "apps/backend/app/storage_paths.py"
      via: "documented separation between install directory and `%LOCALAPPDATA%` data root"
      pattern: "LOCALAPPDATA"
    - from: "apps/desktop/README.md"
      to: "package.json"
      via: "documented root release commands"
      pattern: "desktop:release:win"
---

<objective>
Document the install and manual upgrade path so v1.1 has a safe, predictable replacement story without introducing auto-update infrastructure.

Purpose: Close the user-facing distribution contract for Phase 8 by spelling out how installation, shortcuts, Program Files placement, and upgrade-safe data retention work.
Output: Install/upgrade runbook plus aligned desktop and quick-start documentation.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-installer-release-delivery/08-CONTEXT.md
@.planning/phases/07-bundled-runtime-persistent-data/07-02-SUMMARY.md
@apps/desktop/README.md
@EASY-SETUP.md
@apps/backend/app/storage_paths.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Windows install and manual upgrade runbook</name>
  <read_first>.planning/phases/08-installer-release-delivery/08-CONTEXT.md, .planning/phases/07-bundled-runtime-persistent-data/07-02-SUMMARY.md, apps/backend/app/storage_paths.py, EASY-SETUP.md</read_first>
  <files>docs/release/windows-install-upgrade.md</files>
  <acceptance_criteria>
    - `docs/release/windows-install-upgrade.md` includes sections named `First-time install`, `Installed app location`, `User data location`, `Upgrade with a newer installer`, and `What not to do`
    - The document names `%ProgramFiles%` as the install target
    - The document names `%LOCALAPPDATA%` as the user-data area that survives reinstall and upgrade
    - The document states that upgrades are performed by running a newer installer over the existing app and that v1.1 does not support side-by-side installs
    - The document explicitly tells the user not to delete the local data directory when updating
  </acceptance_criteria>
  <action>
    Create `docs/release/windows-install-upgrade.md` per D-05 through D-09 with concrete end-user instructions:
    - `First-time install`: run `ResumeMatcher-Setup-<version>.exe`, accept elevation, finish install, then launch from Desktop or Start menu
    - `Installed app location`: machine-wide install under `%ProgramFiles%`
    - `User data location`: stable app data under `%LOCALAPPDATA%`, separate from the install directory, so resumes, jobs, applications, config, and backups survive reinstall
    - `Upgrade with a newer installer`: close the app, run the newer installer, let it replace the existing install, then reopen the app and confirm data is still present
    - `What not to do`: do not maintain multiple side-by-side app installs, do not delete `%LOCALAPPDATA%` data during upgrade, do not expect auto-update in v1.1

    Keep this document user-facing and concrete. Do not turn it into a Phase 9 release checklist or diagnostics guide.
  </action>
  <verify>
    <automated>rg -n "First-time install|Installed app location|User data location|Upgrade with a newer installer|What not to do|%ProgramFiles%|%LOCALAPPDATA%|side-by-side|auto-update" docs/release/windows-install-upgrade.md</automated>
  </verify>
  <done>The repository contains a clear Windows install and replacement-upgrade path that protects the Phase 7 stable data root and avoids ambiguous side-by-side installs.</done>
</task>

<task type="auto">
  <name>Task 2: Align desktop and quick-start docs to the installer workflow</name>
  <read_first>docs/release/windows-install-upgrade.md, apps/desktop/README.md, EASY-SETUP.md, package.json</read_first>
  <files>apps/desktop/README.md, EASY-SETUP.md</files>
  <acceptance_criteria>
    - `apps/desktop/README.md` contains `npm run desktop:release:win` and `npm run desktop:release:win:dir`
    - `apps/desktop/README.md` mentions the installer output directory `dist/desktop-installer`
    - `EASY-SETUP.md` mentions the Windows installer `.exe` path instead of a developer setup requirement for the target user
    - `EASY-SETUP.md` points to `docs/release/windows-install-upgrade.md` for manual updates
  </acceptance_criteria>
  <action>
    Update `apps/desktop/README.md` to reflect the new installer workflow:
    - keep build and test guidance
    - add release commands `npm run desktop:release:win` and `npm run desktop:release:win:dir`
    - state installer artifacts land in `dist/desktop-installer`

    Update `EASY-SETUP.md` for the non-technical Windows user:
    - distinguish developer setup from Windows installer usage
    - point the user to the generated installer `.exe`
    - refer manual updates to `docs/release/windows-install-upgrade.md`

    Do not rewrite the general developer onboarding docs outside these files.
  </action>
  <verify>
    <automated>rg -n "desktop:release:win|desktop:release:win:dir|dist/desktop-installer" apps/desktop/README.md && rg -n "installer|windows-install-upgrade.md|\\.exe" EASY-SETUP.md</automated>
  </verify>
  <done>The desktop workspace docs and high-level setup guidance both point to the same installer and manual update path that Phase 8 defines.</done>
</task>

</tasks>

<verification>
Check that the install/upgrade doc names the exact Windows locations and replacement-upgrade flow, then confirm the desktop README and quick-start guide point to the same commands and documentation.
</verification>

<success_criteria>
The repository documents a normal Windows install and manual upgrade path that preserves local data, avoids side-by-side installs, and matches the packaging outputs defined in Phase 8.
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-release-delivery/08-03-SUMMARY.md`
</output>
