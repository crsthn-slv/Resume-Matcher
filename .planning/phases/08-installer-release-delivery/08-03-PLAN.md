---
phase: 08-installer-release-delivery
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - apps/desktop/electron-builder.json
  - apps/desktop/README.md
  - docs/releases/windows-desktop.md
  - docs/releases/windows-upgrade.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INST-02
  - REL-03
must_haves:
  truths:
    - The installer creates the user-facing launch points expected for a normal Windows app.
    - The v1.1 upgrade path is explicitly manual and preserves the stable data location from Phase 7.
    - Maintainers and the end user have a clear documented path for installing a new version over an old one.
  artifacts:
    - path: apps/desktop/electron-builder.json
      provides: Shortcut and install-behavior settings for the Windows installer
      contains: "\"perMachine\": true"
    - path: docs/releases/windows-upgrade.md
      provides: Manual installer-based upgrade instructions for v1.1
      contains: "Run the newer installer"
    - path: apps/desktop/README.md
      provides: Concise pointer to install/upgrade documentation for maintainers
      contains: "windows-upgrade.md"
  key_links:
    - from: docs/releases/windows-upgrade.md
      to: .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md
      via: upgrades preserve user data outside the install directory
      pattern: "stable per-user|data"
    - from: apps/desktop/electron-builder.json
      to: docs/releases/windows-desktop.md
      via: documented installer behavior matches actual builder config
      pattern: "Desktop|Start Menu|Program Files"
---

<objective>
Finish the installer-facing delivery contract: shortcuts, install behavior, and the manual upgrade runbook for v1.1.

Purpose: Even with packaging and CI in place, the phase is not complete until the installer behavior and update story are documented clearly enough for real user delivery.
Output: Final installer behavior config and end-to-end release/upgrade documentation for v1.1.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-installer-release-delivery/08-CONTEXT.md
@apps/desktop/electron-builder.json
@docs/releases/windows-desktop.md
@apps/desktop/README.md
@.planning/phases/07-bundled-runtime-persistent-data/07-VERIFICATION.md

<interfaces>
Phase 8 decisions already locked:
- installer must be one NSIS `.exe`
- install target must be machine-wide in `Program Files`
- Desktop and Start Menu entry points are required
- updates in v1.1 happen by running a newer installer manually
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize installer behavior settings for Program Files and shortcut creation</name>
  <files>apps/desktop/electron-builder.json, apps/desktop/README.md</files>
  <read_first>apps/desktop/electron-builder.json, apps/desktop/package.json, .planning/phases/08-installer-release-delivery/08-CONTEXT.md</read_first>
  <action>Update `apps/desktop/electron-builder.json` so the NSIS config explicitly reflects the locked install behavior: machine-wide installation under `Program Files`, elevation enabled, Desktop shortcut created, Start Menu entry created, and upgrade installs allowed to replace the existing app instead of creating side-by-side installs. Use concrete Electron Builder NSIS fields rather than leaving shortcut behavior implicit. Update `apps/desktop/README.md` with a short section that names the user-facing result clearly: one installer `.exe`, installation under `Program Files`, and launch through Desktop or Start Menu shortcuts. Do not document internal backend or runtime executables as something the user should run.</action>
  <verify>rg -n "perMachine|allowElevation|shortcut|Start Menu|Program Files" apps/desktop/electron-builder.json apps/desktop/README.md</verify>
  <acceptance_criteria>
    - `apps/desktop/electron-builder.json` contains `perMachine`
    - `apps/desktop/electron-builder.json` contains `allowElevation`
    - `apps/desktop/electron-builder.json` contains Desktop shortcut settings
    - `apps/desktop/electron-builder.json` contains Start Menu shortcut settings
    - `apps/desktop/README.md` contains `Program Files`
    - `apps/desktop/README.md` describes one installer `.exe`
  </acceptance_criteria>
  <done>The installer behavior now matches the exact Windows install experience chosen in discuss-phase.</done>
</task>

<task type="auto">
  <name>Task 2: Write the manual upgrade runbook for v1.1 installer-based updates</name>
  <files>docs/releases/windows-desktop.md, docs/releases/windows-upgrade.md, apps/desktop/README.md</files>
  <read_first>docs/releases/windows-desktop.md, apps/desktop/README.md, .planning/phases/07-bundled-runtime-persistent-data/07-VERIFICATION.md, .planning/phases/08-installer-release-delivery/08-CONTEXT.md</read_first>
  <action>Create `docs/releases/windows-upgrade.md` with the exact sections `When to use this`, `Before installing a new version`, `Run the newer installer`, `What should remain unchanged`, and `If something goes wrong`. Document the v1.1 manual update contract explicitly: close the app, run the newer installer, keep the existing stable user-data location untouched, relaunch from the normal shortcut, and verify the existing data is still present. Update `docs/releases/windows-desktop.md` to link to the upgrade runbook, and update `apps/desktop/README.md` to point maintainers at both the release and upgrade docs. Do not introduce auto-update setup, signed-feed publishing, or silent updates in this phase.</action>
  <verify>rg -n "When to use this|Before installing a new version|Run the newer installer|What should remain unchanged|If something goes wrong|windows-upgrade.md" docs/releases/windows-upgrade.md docs/releases/windows-desktop.md apps/desktop/README.md</verify>
  <acceptance_criteria>
    - `docs/releases/windows-upgrade.md` contains `When to use this`
    - `docs/releases/windows-upgrade.md` contains `Run the newer installer`
    - `docs/releases/windows-upgrade.md` contains `What should remain unchanged`
    - `docs/releases/windows-desktop.md` references `windows-upgrade.md`
    - `apps/desktop/README.md` references both release and upgrade docs
  </acceptance_criteria>
  <done>The v1.1 manual upgrade path is documented clearly enough to ship new installers without surprising the user or wiping local data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "perMachine|allowElevation|shortcut|Program Files" apps/desktop/electron-builder.json apps/desktop/README.md`
- [ ] `rg -n "Run the newer installer|What should remain unchanged|If something goes wrong|windows-upgrade.md" docs/releases/windows-upgrade.md docs/releases/windows-desktop.md apps/desktop/README.md`
- [ ] Docs describe manual installer replacement and preserved user data, not auto-update
</verification>

<success_criteria>
The installer behavior and the v1.1 manual upgrade path are both explicit, documented, and aligned with the actual packaging config.
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-release-delivery/08-03-SUMMARY.md`
</output>
