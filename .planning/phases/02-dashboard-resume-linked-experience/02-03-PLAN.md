---
phase: 02-dashboard-resume-linked-experience
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/frontend/app/(default)/resumes/[id]/page.tsx
  - apps/frontend/components/ui/dialog.tsx
  - apps/frontend/messages/en.json
  - apps/frontend/messages/es.json
  - apps/frontend/messages/ja.json
  - apps/frontend/messages/pt-BR.json
  - apps/frontend/messages/zh.json
autonomous: true
requirements:
  - APPL-05
  - APPL-06
  - DASH-03
user_setup: []
must_haves:
  truths:
    - The resume viewer page contains the linked application experience for details, metadata editing, and status changes.
    - Users can create a linked application from the resume page when one does not exist yet.
    - Status changes still use the dedicated status endpoint and application history remains reverse-ordered in the UI.
  artifacts:
    - apps/frontend/app/(default)/resumes/[id]/page.tsx renders an application area or delegates to dedicated application components.
    - Any dialogs or forms used for application editing/creation follow the existing shared dialog pattern.
    - Localized strings exist for the new application panel, create/edit actions, and status flows.
  key_links:
    - The resume viewer uses the application data helpers from Plan 02-01.
    - Metadata editing calls application PATCH while status changes call the dedicated status endpoint.
---

<objective>
Add the linked application details, creation, edit, and status-management experience inside the tailored-resume viewer page.

Purpose: Make the resume page the place where the user actually manages the application tied to that tailored resume.
Output: An application panel on `/resumes/[id]` with create, view, edit, and status actions wired to the backend contract.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md
@.planning/phases/02-dashboard-resume-linked-experience/02-RESEARCH.md
@apps/frontend/app/(default)/resumes/[id]/page.tsx
@apps/frontend/lib/api/applications.ts
@apps/frontend/components/ui/dialog.tsx
@apps/frontend/components/ui/confirm-dialog.tsx
@apps/frontend/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a linked application area to the resume viewer page</name>
  <files>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/messages/en.json, apps/frontend/messages/es.json, apps/frontend/messages/ja.json, apps/frontend/messages/pt-BR.json, apps/frontend/messages/zh.json</files>
  <read_first>apps/frontend/app/(default)/resumes/[id]/page.tsx, .planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Add a dedicated application block/area to the resume viewer page that appears alongside the current resume experience. When a linked application exists, show the required details there: company, role, status, notes, `job_url`, linked job description when available, and reverse-ordered status history. When no linked application exists, show a clear `Create application` entry point in this same area.</action>
  <verify>rg -n "Create application|status history|job_url|job description|application" 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/messages/*.json</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/resumes/[id]/page.tsx` contains a dedicated application area
    - The no-application state contains a clear create action
    - The existing-application state includes reverse-ordered history display
    - New viewer/application strings exist in the locale files touched by the feature
  </acceptance_criteria>
  <done>The resume viewer page can show either application details or a creation entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Build create/edit application metadata flows from the resume viewer</name>
  <files>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/components/ui/dialog.tsx</files>
  <read_first>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/components/ui/dialog.tsx, apps/frontend/lib/api/applications.ts</read_first>
  <action>Implement the UI flow that creates a linked application from the resume page when none exists and edits metadata when one does exist. Reuse the existing dialog primitive if a modal keeps the page cleaner. The create/edit flow must capture and update `company`, `role`, `job_url`, notes, and any other MVP metadata needed by the existing backend contract, while keeping status changes out of the metadata form.</action>
  <verify>rg -n "createApplication|updateApplication|Dialog|company|role|job_url|notes" 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/components/ui/dialog.tsx</verify>
  <acceptance_criteria>
    - Resume viewer UI calls `createApplication` for the no-linked-application path
    - Resume viewer UI calls `updateApplication` for metadata edits
    - Metadata create/edit flow does not mutate status directly
  </acceptance_criteria>
  <done>The user can create and edit application metadata without leaving the resume page.</done>
</task>

<task type="auto">
  <name>Task 3: Wire dedicated status changes from the resume viewer page</name>
  <files>apps/frontend/app/(default)/resumes/[id]/page.tsx</files>
  <read_first>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/lib/api/applications.ts, .planning/phases/01-applications-backend-foundation/01-CONTEXT.md</read_first>
  <action>Implement status updates from the resume viewer page using the dedicated application status endpoint only. Provide the user an explicit status action/control in the application area and refresh the displayed application details/history after changes. Keep the contract aligned with Phase 1: metadata patch is separate from status mutation, and the UI should render the resulting history in reverse chronological order.</action>
  <verify>rg -n "updateApplicationStatus|status_history|reverse|sort|status" 'apps/frontend/app/(default)/resumes/[id]/page.tsx'</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/resumes/[id]/page.tsx` calls `updateApplicationStatus`
    - Displayed history is rendered in reverse chronological order
    - Status changes are not routed through metadata PATCH logic
  </acceptance_criteria>
  <done>The resume viewer can safely change application status while preserving the backend audit model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- --file apps/frontend/app/(default)/resumes/[id]/page.tsx`
- [ ] `rg -n "createApplication|updateApplication|updateApplicationStatus|status_history" apps/frontend/app/(default)/resumes/[id]/page.tsx`
- [ ] Resume viewer remains the normal tailored-resume page with an embedded application management area
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Resume viewer page becomes the primary place to manage the linked application without breaking the existing resume flow
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-resume-linked-experience/02-03-SUMMARY.md`
</output>
