---
phase: 02-dashboard-resume-linked-experience
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/frontend/app/(default)/dashboard/page.tsx
  - apps/frontend/components/ui/card.tsx
  - apps/frontend/messages/en.json
  - apps/frontend/messages/es.json
  - apps/frontend/messages/ja.json
  - apps/frontend/messages/pt-BR.json
  - apps/frontend/messages/zh.json
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05
  - DASH-01
  - DASH-02
user_setup: []
must_haves:
  truths:
    - The dashboard remains the existing tailored-resume dashboard, not a separate applications page.
    - Tailored-resume entries show linked application state with a status badge only.
    - Search and status filters operate over resume-linked application data while keeping resumes as the visible result items.
  artifacts:
    - apps/frontend/app/(default)/dashboard/page.tsx renders status badges and filter controls for resume-linked applications.
    - Localized dashboard strings exist for new filter/search/status-badge UI.
  key_links:
    - Dashboard filter state is wired to the application data-loading layer from Plan 02-01.
    - Visible dashboard items remain tailored resumes and still route to `/resumes/[id]`.
---

<objective>
Integrate resume-linked application state, search, and filters into the existing dashboard cards.

Purpose: Make the dashboard visibly application-aware without turning it into a new tracker screen.
Output: Application-status badges on tailored-resume entries plus search and status filtering over the joined resume/application dataset.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md
@.planning/phases/02-dashboard-resume-linked-experience/02-RESEARCH.md
@apps/frontend/app/(default)/dashboard/page.tsx
@apps/frontend/components/ui/card.tsx
@apps/frontend/lib/api/applications.ts
@apps/frontend/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard search and application-status filter controls</name>
  <files>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/messages/en.json, apps/frontend/messages/es.json, apps/frontend/messages/ja.json, apps/frontend/messages/pt-BR.json, apps/frontend/messages/zh.json</files>
  <read_first>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/messages/en.json, .planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Add dashboard controls that let the user search by company or role and filter by one or more application statuses while staying inside the current dashboard page. The visible results must remain tailored-resume entries. Wire any new labels/placeholders/status text through the existing translation message files for all supported locales touched by the repo.</action>
  <verify>rg -n "search|status|Every status|application" 'apps/frontend/app/(default)/dashboard/page.tsx' apps/frontend/messages/*.json</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/dashboard/page.tsx` contains a search control for company/role filtering
    - `apps/frontend/app/(default)/dashboard/page.tsx` contains status-filter UI
    - New dashboard application-filter strings exist in all locale files modified by the feature
  </acceptance_criteria>
  <done>The dashboard can narrow tailored-resume cards using linked application search and status filters.</done>
</task>

<task type="auto">
  <name>Task 2: Add simple application status badges to tailored-resume cards</name>
  <files>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/components/ui/card.tsx</files>
  <read_first>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/components/ui/card.tsx, .planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md</read_first>
  <action>Render a simple status badge on tailored-resume entries when a linked application exists. Keep the card treatment lightweight: no extra company, role, notes, or description blocks on the dashboard. Adjust card layout only as much as needed to keep the badge readable and consistent with the current Swiss-style cards.</action>
  <verify>rg -n "badge|status|application" 'apps/frontend/app/(default)/dashboard/page.tsx' apps/frontend/components/ui/card.tsx</verify>
  <acceptance_criteria>
    - Tailored-resume cards display a linked application status badge
    - The dashboard card does not add company/role/job-description text by default
    - Card clicks still navigate to `/resumes/{id}`
  </acceptance_criteria>
  <done>Dashboard cards communicate linked application state without becoming a separate tracker UI.</done>
</task>

<task type="auto">
  <name>Task 3: Keep dashboard ordering and visibility aligned with resume-linked application results</name>
  <files>apps/frontend/app/(default)/dashboard/page.tsx</files>
  <read_first>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/lib/api/applications.ts, .planning/ROADMAP.md</read_first>
  <action>Ensure the dashboard's tailored-resume results follow the application-aware search/status pipeline and preserve the required ordering by most recent application activity where applicable. Define and implement the intended behavior for resumes without linked applications when filters are active versus inactive, while staying within the locked dashboard model.</action>
  <verify>rg -n "updated_at|sort|filter|tailoredResumes|application" 'apps/frontend/app/(default)/dashboard/page.tsx'</verify>
  <acceptance_criteria>
    - Dashboard filtering produces tailored-resume results based on linked application data
    - Resume-linked results are ordered by the linked application's recency when filter/search mode is active
    - Behavior for resumes without linked applications is explicit in the implementation rather than accidental
  </acceptance_criteria>
  <done>The dashboard behaves predictably as an application-aware resume list.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- --file apps/frontend/app/(default)/dashboard/page.tsx --file apps/frontend/components/ui/card.tsx`
- [ ] `rg -n "application|status|search|filter" apps/frontend/app/(default)/dashboard/page.tsx apps/frontend/messages/*.json`
- [ ] Dashboard still uses tailored-resume cards as the primary visible unit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dashboard remains familiar while becoming application-aware through resume-linked badges and filters
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-resume-linked-experience/02-02-SUMMARY.md`
</output>
