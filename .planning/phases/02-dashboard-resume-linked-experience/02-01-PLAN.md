---
phase: 02-dashboard-resume-linked-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/lib/api/applications.ts
  - apps/frontend/lib/api/index.ts
  - apps/frontend/app/(default)/dashboard/page.tsx
  - apps/frontend/app/(default)/resumes/[id]/page.tsx
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05
  - DASH-01
user_setup: []
must_haves:
  truths:
    - Frontend has typed access to application list/detail/create/update/status endpoints.
    - Dashboard and resume-viewer routes can resolve a linked application by `resume_id`.
    - Data-loading logic is ready for search and status filters without introducing a separate applications surface.
  artifacts:
    - apps/frontend/lib/api/applications.ts defines typed application helpers and models.
    - apps/frontend/lib/api/index.ts re-exports the new applications helpers.
    - apps/frontend/app/(default)/dashboard/page.tsx and apps/frontend/app/(default)/resumes/[id]/page.tsx consume shared application lookup helpers or state.
  key_links:
    - Frontend application types align with the backend payloads from `apps/backend/app/routers/applications.py`.
    - Resume-linked lookup is keyed by `resume_id`, not by ad hoc URL or localStorage state.
---

<objective>
Build the frontend data layer for resume-linked application tracking before changing the visible dashboard and viewer UX.

Purpose: Make application reads and mutations available to the dashboard and resume viewer in a typed, reusable way.
Output: Shared API helpers, frontend types, and route-level loading hooks/state for linked application data.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md
@.planning/phases/02-dashboard-resume-linked-experience/02-RESEARCH.md
@apps/frontend/lib/api/client.ts
@apps/frontend/lib/api/resume.ts
@apps/frontend/lib/api/index.ts
@apps/frontend/app/(default)/dashboard/page.tsx
@apps/frontend/app/(default)/resumes/[id]/page.tsx
@apps/backend/app/routers/applications.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed frontend application API helpers</name>
  <files>apps/frontend/lib/api/applications.ts, apps/frontend/lib/api/index.ts</files>
  <read_first>apps/frontend/lib/api/client.ts, apps/frontend/lib/api/resume.ts, apps/backend/app/routers/applications.py, apps/backend/app/schemas/models.py</read_first>
  <action>Create `apps/frontend/lib/api/applications.ts` with typed models and helpers for the Phase 1 backend contract: list, detail, create, metadata patch, and status change. Include request/response types for status history, display-ready list items, and application config fields the frontend actually consumes. Re-export the helpers from `apps/frontend/lib/api/index.ts` using the same explicit style already used for resume/config APIs.</action>
  <verify>rg -n "fetchApplications|fetchApplication|createApplication|updateApplication|updateApplicationStatus" apps/frontend/lib/api/applications.ts apps/frontend/lib/api/index.ts</verify>
  <acceptance_criteria>
    - `apps/frontend/lib/api/applications.ts` exists
    - `apps/frontend/lib/api/applications.ts` contains `export async function fetchApplications`
    - `apps/frontend/lib/api/applications.ts` contains `export async function fetchApplication`
    - `apps/frontend/lib/api/applications.ts` contains `export async function createApplication`
    - `apps/frontend/lib/api/applications.ts` contains `export async function updateApplication`
    - `apps/frontend/lib/api/applications.ts` contains `export async function updateApplicationStatus`
    - `apps/frontend/lib/api/index.ts` re-exports the application helpers
  </acceptance_criteria>
  <done>The frontend can call the application backend through typed, shared helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add resume-linked application lookup helpers for dashboard and viewer routes</name>
  <files>apps/frontend/lib/api/applications.ts, apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/app/(default)/resumes/[id]/page.tsx</files>
  <read_first>apps/frontend/lib/api/applications.ts, apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/app/(default)/resumes/[id]/page.tsx, .planning/phases/02-dashboard-resume-linked-experience/02-CONTEXT.md</read_first>
  <action>Add the minimal shared logic needed to resolve applications by `resume_id`. This can live in `applications.ts` or route-local helpers, but it must support: (a) fetching a filtered/sorted application list for dashboard use, and (b) resolving the current resume's linked application on `/resumes/[id]`. Keep this keyed by `resume_id` and avoid introducing route params, query strings, or localStorage just to carry application ids around.</action>
  <verify>rg -n "resume_id|Map\\(|find\\(|fetchApplications" apps/frontend/lib/api/applications.ts 'apps/frontend/app/(default)/dashboard/page.tsx' 'apps/frontend/app/(default)/resumes/[id]/page.tsx'</verify>
  <acceptance_criteria>
    - Dashboard-side code resolves linked applications by `resume_id`
    - Resume viewer code contains a lookup path for the current resume's linked application
    - No new localStorage key or URL parameter is introduced for application identity
  </acceptance_criteria>
  <done>Both main routes have a stable way to find the application linked to a resume.</done>
</task>

<task type="auto">
  <name>Task 3: Prepare route-level loading state for search and status filters</name>
  <files>apps/frontend/app/(default)/dashboard/page.tsx</files>
  <read_first>apps/frontend/app/(default)/dashboard/page.tsx, apps/frontend/lib/api/applications.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Refactor dashboard loading/state as needed so application-aware search and status filtering can be layered on in Plan 02 without fighting the existing resume-loading flow. This may include separating raw tailored resumes from visible items, adding application filter state, and creating a normalized joined structure for rendering. Do not fully build the visible filter UI in this plan; the goal is stable data plumbing for the next wave.</action>
  <verify>rg -n "search|filter|status|visible|joined|application" 'apps/frontend/app/(default)/dashboard/page.tsx'</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/dashboard/page.tsx` contains state or helpers for application-aware filtering
    - Dashboard loading keeps tailored resumes as the primary visible unit
    - The data flow can support search and status filters without adding a separate applications list view
  </acceptance_criteria>
  <done>The dashboard route is structurally ready for Phase 2 filtering and badge integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- --file apps/frontend/app/(default)/dashboard/page.tsx --file apps/frontend/app/(default)/resumes/[id]/page.tsx --file apps/frontend/lib/api/applications.ts`
- [ ] `rg -n "fetchApplications|resume_id|updateApplicationStatus" apps/frontend/lib/api/applications.ts apps/frontend/lib/api/index.ts apps/frontend/app/(default)/dashboard/page.tsx apps/frontend/app/(default)/resumes/[id]/page.tsx`
- [ ] Application data loading stays resume-centric and does not create a standalone tracker surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend data access is ready for resume-linked application UI work in later plans
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-resume-linked-experience/02-01-SUMMARY.md`
</output>
