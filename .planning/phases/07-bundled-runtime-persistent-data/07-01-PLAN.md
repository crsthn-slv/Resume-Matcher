---
phase: 07-bundled-runtime-persistent-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/desktop/package.json
  - apps/backend/pyproject.toml
  - apps/backend/desktop.spec
  - apps/desktop/src/runtime/packaged-layout.ts
  - scripts/desktop/build-runtime.mjs
  - scripts/desktop/build-backend.py
autonomous: true
requirements:
  - DATA-01
user_setup: []
must_haves:
  truths:
    - A maintainer can build a desktop runtime bundle that already contains the frontend server, backend executable, embedded Node runtime, and bundled Chromium without requiring those runtimes on the target machine.
    - The packaged runtime layout is explicit and stable so later Electron code can launch known local artifacts instead of guessing file locations.
    - The build output remains additive around the existing Next.js plus FastAPI app per D-01 and D-02.
  artifacts:
    - path: scripts/desktop/build-runtime.mjs
      provides: Orchestrated runtime build that emits frontend, backend, Node, and Playwright assets into one bundle tree
      contains: "dist/desktop/runtime"
    - path: apps/backend/desktop.spec
      provides: PyInstaller onedir spec for the backend executable
      contains: "rm-backend"
    - path: apps/desktop/src/runtime/packaged-layout.ts
      provides: Shared runtime path contract for packaged frontend/backend/node/playwright assets
      contains: "resolvePackagedRuntimeLayout"
  key_links:
    - from: scripts/desktop/build-runtime.mjs
      to: apps/frontend/package.json
      via: Next standalone production build
      pattern: "npm run build"
    - from: scripts/desktop/build-backend.py
      to: apps/backend/desktop.spec
      via: PyInstaller execution
      pattern: "PyInstaller"
    - from: apps/desktop/src/runtime/packaged-layout.ts
      to: scripts/desktop/build-runtime.mjs
      via: shared dist/runtime directory names
      pattern: "runtime/frontend|runtime/backend|runtime/bin|runtime/playwright"
---

<objective>
Define the packaged runtime build pipeline and artifact layout for the Windows desktop distribution.

Purpose: Phase 7 cannot keep relying on repo-relative source trees or developer-installed runtimes. This plan establishes the exact build outputs that later plans will persist and launch.
Output: Maintainer build scripts, a backend PyInstaller spec, and a shared packaged-runtime path contract for frontend, backend, Node, and Playwright assets.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md
@.planning/phases/06-desktop-shell-lifecycle/06-02-SUMMARY.md
@Dockerfile
@docker/start.sh
@package.json
@apps/desktop/package.json
@apps/frontend/package.json
@apps/frontend/next.config.ts
@apps/backend/pyproject.toml
@apps/backend/app/main.py
@apps/backend/app/pdf.py

<interfaces>
From `apps/frontend/next.config.ts`:
```ts
const BACKEND_ORIGIN = process.env.BACKEND_ORIGIN || 'http://127.0.0.1:8000';
output: 'standalone';
```

From `apps/desktop/src/runtime/command-factory.ts`:
```ts
export interface RuntimeCommand {
  command: string;
  args: string[];
  cwd: string;
  env: NodeJS.ProcessEnv;
}
```

From `Dockerfile` and `docker/start.sh`:
```text
Frontend production runtime = Next standalone server.js + .next/static + public
Backend production runtime = python -m uvicorn app.main:app
Playwright browser setup currently installs chromium before runtime startup
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the backend bundle spec and runtime build scripts</name>
  <files>apps/backend/pyproject.toml, apps/backend/desktop.spec, scripts/desktop/build-runtime.mjs, scripts/desktop/build-backend.py</files>
  <read_first>apps/backend/pyproject.toml, apps/backend/app/main.py, apps/backend/app/pdf.py, apps/frontend/package.json, apps/frontend/next.config.ts, Dockerfile, docker/start.sh, .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md</read_first>
  <action>Add a desktop-only backend build dependency in `apps/backend/pyproject.toml` so the maintainer can run PyInstaller from the existing backend project; use an optional dependency group named `desktop` that pins `pyinstaller` and `pyinstaller-hooks-contrib`, not a new global toolchain. Create `apps/backend/desktop.spec` for an onedir executable named `rm-backend` per D-01, with `app/main.py` as the entry script, `pathex` including `apps/backend`, and explicit data collection for the `app` package plus Playwright metadata so the compiled backend can import the current API, routers, prompts, and PDF renderer. Create `scripts/desktop/build-backend.py` to run `python -m PyInstaller apps/backend/desktop.spec --noconfirm --clean --distpath dist/desktop/runtime/backend --workpath .tmp/desktop-pyinstaller --specpath apps/backend`, then fail fast if `dist/desktop/runtime/backend/rm-backend/rm-backend.exe` is missing. Create `scripts/desktop/build-runtime.mjs` to do the full runtime build in this order: remove `dist/desktop/runtime`, run `npm --prefix apps/frontend run build`, copy the Next standalone output into `dist/desktop/runtime/frontend`, copy `apps/frontend/public` and `.next/static`, copy the host machine Node executable into `dist/desktop/runtime/bin/node.exe`, run `uv run --project apps/backend --extra desktop python scripts/desktop/build-backend.py`, and install Playwright Chromium into `dist/desktop/runtime/playwright` by setting `PLAYWRIGHT_BROWSERS_PATH` to that directory before calling `python -m playwright install chromium`. Do not introduce Docker, first-boot installers, or an alternate web host per D-02 and deferred installer scope.</action>
  <verify>node scripts/desktop/build-runtime.mjs --verify-layout</verify>
  <acceptance_criteria>
    - `apps/backend/pyproject.toml` contains `[project.optional-dependencies]` and `desktop = [`
    - `apps/backend/desktop.spec` contains `name='rm-backend'` or `name = 'rm-backend'`
    - `scripts/desktop/build-backend.py` contains `dist/desktop/runtime/backend`
    - `scripts/desktop/build-runtime.mjs` contains `dist/desktop/runtime/frontend`
    - `scripts/desktop/build-runtime.mjs` contains `runtime/bin/node.exe`
    - `scripts/desktop/build-runtime.mjs` contains `PLAYWRIGHT_BROWSERS_PATH`
  </acceptance_criteria>
  <done>The repo has a deterministic build script that produces the backend executable, frontend standalone runtime, embedded Node binary, and bundled Chromium in one runtime tree.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the packaged runtime layout contract and maintainer entrypoints</name>
  <files>package.json, apps/desktop/package.json, apps/desktop/src/runtime/packaged-layout.ts</files>
  <read_first>package.json, apps/desktop/package.json, apps/desktop/src/runtime/command-factory.ts, apps/desktop/src/config.ts, scripts/desktop/build-runtime.mjs, .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md</read_first>
  <action>Modify the workspace `package.json` to add maintainer scripts named exactly `desktop:bundle-runtime`, `desktop:bundle-runtime:verify`, and `desktop:bundle-runtime:clean` that call `node scripts/desktop/build-runtime.mjs` with explicit flags. Update `apps/desktop/package.json` with a non-runtime script named exactly `bundle:runtime` that forwards to the root build script so the desktop workspace owns the packaging entrypoint without duplicating logic. Create `apps/desktop/src/runtime/packaged-layout.ts` with a single exported helper `resolvePackagedRuntimeLayout(baseDir: string)` that returns these exact relative targets: `frontendServer = runtime/frontend/server.js`, `frontendStaticDir = runtime/frontend/.next/static`, `frontendPublicDir = runtime/frontend/public`, `backendExecutable = runtime/backend/rm-backend/rm-backend.exe`, `nodeExecutable = runtime/bin/node.exe`, and `playwrightBrowsersDir = runtime/playwright`. Also export a `PACKAGED_RUNTIME_VERSION = 1` constant so later plans can reject stale runtime layouts deterministically. Do not make Electron launch anything in this task; this file is a contract layer for the next plan.</action>
  <verify>rg -n "desktop:bundle-runtime|desktop:bundle-runtime:verify|bundle:runtime|resolvePackagedRuntimeLayout|PACKAGED_RUNTIME_VERSION" package.json apps/desktop/package.json apps/desktop/src/runtime/packaged-layout.ts</verify>
  <acceptance_criteria>
    - `package.json` contains `desktop:bundle-runtime`
    - `package.json` contains `desktop:bundle-runtime:verify`
    - `apps/desktop/package.json` contains `bundle:runtime`
    - `apps/desktop/src/runtime/packaged-layout.ts` contains `resolvePackagedRuntimeLayout`
    - `apps/desktop/src/runtime/packaged-layout.ts` contains `runtime/frontend/server.js`
    - `apps/desktop/src/runtime/packaged-layout.ts` contains `runtime/backend/rm-backend/rm-backend.exe`
    - `apps/desktop/src/runtime/packaged-layout.ts` contains `runtime/bin/node.exe`
    - `apps/desktop/src/runtime/packaged-layout.ts` contains `runtime/playwright`
  </acceptance_criteria>
  <done>The runtime bundle shape is encoded in source and exposed through stable maintainer scripts that later plans can consume without rediscovering file locations.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "desktop:bundle-runtime|bundle:runtime|resolvePackagedRuntimeLayout|runtime/bin/node.exe|PLAYWRIGHT_BROWSERS_PATH" package.json apps/desktop/package.json apps/desktop/src/runtime/packaged-layout.ts scripts/desktop/build-runtime.mjs`
- [ ] `node scripts/desktop/build-runtime.mjs --verify-layout`
- [ ] `cd apps/backend && uv run --extra desktop python -m PyInstaller --version`
</verification>

<success_criteria>
The repository can produce and describe a single packaged runtime tree that embeds the frontend server, backend executable, Node runtime, and Chromium assets without requiring developer tooling on the target machine.
</success_criteria>

<output>
After completion, create `.planning/phases/07-bundled-runtime-persistent-data/07-01-SUMMARY.md`
</output>
