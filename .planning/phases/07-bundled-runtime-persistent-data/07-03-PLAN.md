---
phase: 07-bundled-runtime-persistent-data
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - apps/desktop/src/runtime/command-factory.ts
  - apps/desktop/src/runtime/supervisor.ts
  - apps/desktop/src/runtime/logging.ts
  - apps/backend/app/pdf.py
  - apps/desktop/tests/runtime-supervisor.test.ts
  - apps/backend/tests/unit/test_pdf_runtime.py
  - apps/desktop/README.md
autonomous: true
requirements:
  - DSK-03
  - DATA-01
  - DATA-03
user_setup: []
must_haves:
  truths:
    - The packaged Electron host launches the bundled frontend and backend artifacts instead of repo source trees, keeping existing product flows available per D-05 and DSK-03.
    - The packaged backend receives explicit environment variables for stable data, backups, and bundled Playwright browsers, so restarts and upgrades keep reading the same data root.
    - PDF export continues to work in the packaged build using bundled Chromium instead of assuming a system browser per D-06.
  artifacts:
    - path: apps/desktop/src/runtime/command-factory.ts
      provides: Production runtime commands for bundled Node and backend executable
      contains: "resolvePackagedRuntimeLayout"
    - path: apps/backend/app/pdf.py
      provides: Packaged Playwright browser resolution for bundled Chromium
      contains: "PLAYWRIGHT_BROWSERS_PATH"
    - path: apps/desktop/tests/runtime-supervisor.test.ts
      provides: Desktop runtime regression coverage for packaged command/env behavior
      contains: "production"
  key_links:
    - from: apps/desktop/src/runtime/command-factory.ts
      to: apps/desktop/src/runtime/packaged-layout.ts
      via: packaged artifact path resolution
      pattern: "resolvePackagedRuntimeLayout"
    - from: apps/desktop/src/runtime/command-factory.ts
      to: apps/backend/app/storage_paths.py
      via: RM_DATA_DIR and RM_BACKUP_DIR environment variables
      pattern: "RM_DATA_DIR|RM_BACKUP_DIR"
    - from: apps/backend/app/pdf.py
      to: dist/desktop/runtime/playwright
      via: bundled browser environment variable
      pattern: "PLAYWRIGHT_BROWSERS_PATH"
---

<objective>
Wire the packaged runtime and stable storage contract into the Electron desktop host and PDF subsystem.

Purpose: After Plans 01 and 02 exist, the desktop app still needs to consume them correctly. This plan makes packaged Electron launches behave like the current product while using local bundled assets and persistent user data.
Output: Production command wiring for bundled artifacts, packaged Playwright browser resolution, regression tests, and maintainer runbook updates.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md
@.planning/phases/07-bundled-runtime-persistent-data/07-01-PLAN.md
@.planning/phases/07-bundled-runtime-persistent-data/07-02-PLAN.md
@apps/desktop/src/runtime/command-factory.ts
@apps/desktop/src/runtime/supervisor.ts
@apps/desktop/src/runtime/logging.ts
@apps/desktop/src/runtime/packaged-layout.ts
@apps/backend/app/pdf.py
@apps/desktop/tests/runtime-supervisor.test.ts

<interfaces>
From `apps/desktop/src/runtime/command-factory.ts`:
```ts
export function createRuntimeCommands(mode: RuntimeMode): RuntimeCommands
```

From `apps/desktop/src/runtime/packaged-layout.ts`:
```ts
export function resolvePackagedRuntimeLayout(baseDir: string): {
  frontendServer: string;
  backendExecutable: string;
  nodeExecutable: string;
  playwrightBrowsersDir: string;
}
```

From `apps/backend/app/pdf.py`:
```py
async def _launch_browser(playwright: Playwright) -> Browser:
    return await playwright.chromium.launch()
```

These contracts must be connected so packaged mode launches real local artifacts and PDF rendering uses the bundled browser tree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire packaged desktop launches to use bundled runtime artifacts and stable user-data env</name>
  <files>apps/desktop/src/runtime/command-factory.ts, apps/desktop/src/runtime/supervisor.ts, apps/desktop/src/runtime/logging.ts, apps/desktop/tests/runtime-supervisor.test.ts</files>
  <read_first>apps/desktop/src/runtime/command-factory.ts, apps/desktop/src/runtime/supervisor.ts, apps/desktop/src/runtime/logging.ts, apps/desktop/src/runtime/packaged-layout.ts, apps/desktop/tests/runtime-supervisor.test.ts, .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Production command creation uses the packaged backend executable and bundled `node.exe` + `server.js` instead of repo-relative cwd commands.
    - Test 2: Production env includes `RM_DATA_DIR`, `RM_BACKUP_DIR`, `PLAYWRIGHT_BROWSERS_PATH`, `BACKEND_ORIGIN`, and `NEXT_PUBLIC_API_URL=/`.
    - Test 3: Desktop logs remain under Electron `userData/logs`, not inside the runtime bundle or install directory.
  </behavior>
  <action>Update `apps/desktop/src/runtime/command-factory.ts` to import `resolvePackagedRuntimeLayout` and use it in production mode. In development mode, keep the current `uv` and `npm run dev` behavior. In production mode, replace repo-relative commands with these exact process launches: backend command = packaged executable from `resolvePackagedRuntimeLayout(process.resourcesPath).backendExecutable`; frontend command = packaged Node binary from `nodeExecutable` with args `[frontendServer]`; frontend env must set `PORT=3000` and `HOSTNAME=127.0.0.1`. For both packaged commands, inject `RM_DATA_DIR = path.join(app.getPath("userData"), "data")`, `RM_BACKUP_DIR = path.join(app.getPath("userData"), "backups")`, and `PLAYWRIGHT_BROWSERS_PATH = resolvePackagedRuntimeLayout(process.resourcesPath).playwrightBrowsersDir` so Plans 01 and 02 connect cleanly. Update `apps/desktop/src/runtime/logging.ts` only as needed so logs continue living under `app.getPath("userData")/logs`, never under `process.resourcesPath`. Extend `apps/desktop/tests/runtime-supervisor.test.ts` to assert the production command and env values explicitly. Do not add install/update behavior or change the backend-first startup order from Phase 6.</action>
  <verify>cd apps/desktop && npm run test -- runtime-supervisor.test.ts</verify>
  <acceptance_criteria>
    - `apps/desktop/src/runtime/command-factory.ts` contains `resolvePackagedRuntimeLayout`
    - `apps/desktop/src/runtime/command-factory.ts` contains `RM_DATA_DIR`
    - `apps/desktop/src/runtime/command-factory.ts` contains `RM_BACKUP_DIR`
    - `apps/desktop/src/runtime/command-factory.ts` contains `PLAYWRIGHT_BROWSERS_PATH`
    - `apps/desktop/src/runtime/command-factory.ts` contains `PORT: '3000'` or `PORT = '3000'`
    - `apps/desktop/tests/runtime-supervisor.test.ts` contains `mode: 'production'`
    - `apps/desktop/src/runtime/logging.ts` contains `app.getPath('userData')`
  </acceptance_criteria>
  <done>The packaged Electron host launches the bundled backend and frontend artifacts and points them at the stable user-data root instead of the repo tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make packaged PDF rendering use bundled Chromium and document the runtime contract</name>
  <files>apps/backend/app/pdf.py, apps/backend/tests/unit/test_pdf_runtime.py, apps/desktop/README.md</files>
  <read_first>apps/backend/app/pdf.py, apps/backend/app/main.py, apps/backend/tests/unit/test_refiner.py, apps/desktop/README.md, .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: When `PLAYWRIGHT_BROWSERS_PATH` is set, browser launch resolution prefers the bundled Playwright cache before searching system Chrome or Edge.
    - Test 2: Missing bundled browsers produce an error message that points to the packaged browser path instead of telling the end user to install Chrome manually.
    - Test 3: Existing development behavior still falls back to system browser discovery when packaged browser env vars are absent.
  </behavior>
  <action>Update `apps/backend/app/pdf.py` so packaged desktop runs treat `PLAYWRIGHT_BROWSERS_PATH` as the primary browser source per D-06. Add a helper that inspects `PLAYWRIGHT_BROWSERS_PATH` and, when Chromium is present there, launches Playwright against that bundled cache first; keep the current system-browser fallback only for development and non-packaged scenarios. Update `_raise_playwright_error()` so the missing-browser message references `PLAYWRIGHT_BROWSERS_PATH` and the packaged runtime instead of telling the user to install a browser manually. Add `apps/backend/tests/unit/test_pdf_runtime.py` covering bundled-path preference, system fallback, and updated error messaging. Update `apps/desktop/README.md` with a packaged-runtime section documenting the exact runtime directories `runtime/frontend`, `runtime/backend`, `runtime/bin`, `runtime/playwright`, plus the env vars `RM_DATA_DIR`, `RM_BACKUP_DIR`, and `PLAYWRIGHT_BROWSERS_PATH`. Do not remove PDF export, and do not assume a globally installed Chrome or Edge in packaged mode.</action>
  <verify>cd apps/backend && uv run --extra dev pytest tests/unit/test_pdf_runtime.py -x</verify>
  <acceptance_criteria>
    - `apps/backend/app/pdf.py` contains `PLAYWRIGHT_BROWSERS_PATH`
    - `apps/backend/app/pdf.py` contains `packaged runtime` or `bundled Chromium`
    - `apps/backend/tests/unit/test_pdf_runtime.py` contains `test_prefers_bundled_playwright_browser_path`
    - `apps/backend/tests/unit/test_pdf_runtime.py` contains `test_missing_bundled_browser_message_references_playwright_browsers_path`
    - `apps/desktop/README.md` contains `runtime/frontend`
    - `apps/desktop/README.md` contains `runtime/backend`
    - `apps/desktop/README.md` contains `PLAYWRIGHT_BROWSERS_PATH`
  </acceptance_criteria>
  <done>Packaged PDF export uses bundled Chromium deterministically, and maintainers have a concrete runtime contract for the desktop bundle.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/desktop && npm run test -- runtime-supervisor.test.ts`
- [ ] `cd apps/backend && uv run --extra dev pytest tests/unit/test_pdf_runtime.py -x`
- [ ] `rg -n "resolvePackagedRuntimeLayout|RM_DATA_DIR|RM_BACKUP_DIR|PLAYWRIGHT_BROWSERS_PATH|runtime/frontend|runtime/backend" apps/desktop/src/runtime/command-factory.ts apps/backend/app/pdf.py apps/desktop/README.md`
</verification>

<success_criteria>
The packaged desktop host runs the bundled frontend and backend against stable user data, and PDF export continues to work through bundled Chromium without requiring a system browser install.
</success_criteria>

<output>
After completion, create `.planning/phases/07-bundled-runtime-persistent-data/07-03-SUMMARY.md`
</output>
