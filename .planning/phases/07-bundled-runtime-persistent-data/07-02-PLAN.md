---
phase: 07-bundled-runtime-persistent-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/app/storage_paths.py
  - apps/backend/app/config.py
  - apps/backend/app/database.py
  - apps/backend/app/main.py
  - apps/backend/tests/integration/test_desktop_storage.py
autonomous: true
requirements:
  - DATA-02
  - DATA-03
user_setup: []
must_haves:
  truths:
    - User data and configuration live outside the repo or install directory in one stable per-user storage root per D-03, D-04, and D-07.
    - Startup can migrate legacy local files into the stable data directory without deleting user data, and backup happens before structural migration per D-08 and D-09.
    - Upgrades keep using the same storage root and schema manifest so new desktop builds continue reading prior data.
  artifacts:
    - path: apps/backend/app/storage_paths.py
      provides: Central storage-root, backup, and migration-manifest helpers
      contains: "RM_DATA_DIR"
    - path: apps/backend/app/config.py
      provides: Settings-backed data, backup, and config path resolution
      contains: "backup_dir"
    - path: apps/backend/tests/integration/test_desktop_storage.py
      provides: Regression coverage for stable path resolution and migration-with-backup
      contains: "test_migrates_legacy_repo_data_into_rm_data_dir"
  key_links:
    - from: apps/backend/app/main.py
      to: apps/backend/app/storage_paths.py
      via: startup storage preparation
      pattern: "prepare_storage"
    - from: apps/backend/app/database.py
      to: apps/backend/app/config.py
      via: settings-backed db path
      pattern: "settings\\.db_path"
---

<objective>
Move backend persistence onto a stable per-user storage root with migration and backup guarantees.

Purpose: Phase 7 is not complete if packaged builds still read or write inside the app install tree. This plan makes the backend storage contract upgrade-safe and recoverable.
Output: Central storage-path helpers, settings-backed stable data directories, startup migration hooks, and integration coverage for migration plus backup behavior.
</objective>

<execution_context>
@/Users/cristhian/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md
@apps/backend/app/config.py
@apps/backend/app/database.py
@apps/backend/app/main.py
@apps/backend/app/config_cache.py
@apps/backend/app/routers/config.py

<interfaces>
From `apps/backend/app/config.py`:
```py
data_dir: Path = Path(__file__).parent.parent / "data"

@property
def db_path(self) -> Path:
    return self.data_dir / "database.json"

@property
def config_path(self) -> Path:
    return self.data_dir / "config.json"
```

From `apps/backend/app/database.py`:
```py
self.db_path = db_path or settings.db_path
self.db_path.parent.mkdir(parents=True, exist_ok=True)
```

These are the current source-of-truth path hooks that must move to a stable per-user root without changing TinyDB usage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the stable storage-root contract for packaged desktop builds</name>
  <files>apps/backend/app/storage_paths.py, apps/backend/app/config.py, apps/backend/app/database.py</files>
  <read_first>apps/backend/app/config.py, apps/backend/app/database.py, apps/backend/app/config_cache.py, apps/backend/app/routers/config.py, .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md</read_first>
  <action>Create `apps/backend/app/storage_paths.py` as the single storage-path source of truth. It must resolve `RM_DATA_DIR` from the environment for packaged builds and fall back to the current repo-local `apps/backend/data` directory for development, preserving D-03 and D-04. Export helpers named exactly `resolve_data_dir()`, `resolve_backup_dir()`, `resolve_migration_manifest_path()`, and `prepare_storage()`. Use a schema manifest file at `<data_dir>/.meta/storage-schema.json` with `schema_version = 1` and store backups under `<backup_dir>/pre-migration/<timestamp>/`. Update `apps/backend/app/config.py` so `Settings.data_dir`, `Settings.db_path`, `Settings.config_path`, and a new `Settings.backup_dir` property all come from these helpers instead of `Path(__file__).parent.parent / "data"`. Update `apps/backend/app/database.py` so any upload or persisted file paths also resolve from `settings.data_dir` and not from repo-relative paths. Keep TinyDB, JSON config files, and current table names unchanged; do not introduce a new database engine or cloud sync.</action>
  <verify>rg -n "RM_DATA_DIR|resolve_data_dir|resolve_backup_dir|storage-schema.json|backup_dir" apps/backend/app/storage_paths.py apps/backend/app/config.py apps/backend/app/database.py</verify>
  <acceptance_criteria>
    - `apps/backend/app/storage_paths.py` contains `RM_DATA_DIR`
    - `apps/backend/app/storage_paths.py` contains `storage-schema.json`
    - `apps/backend/app/config.py` contains `backup_dir`
    - `apps/backend/app/config.py` no longer contains `Path(__file__).parent.parent / "data"`
    - `apps/backend/app/database.py` contains `settings.data_dir`
  </acceptance_criteria>
  <done>The backend has one stable storage-root contract for packaged and development modes, and all primary data/config paths resolve through it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add startup migration plus backup behavior for upgrades</name>
  <files>apps/backend/app/storage_paths.py, apps/backend/app/main.py, apps/backend/tests/integration/test_desktop_storage.py</files>
  <read_first>apps/backend/app/storage_paths.py, apps/backend/app/main.py, apps/backend/tests/conftest.py, apps/backend/tests/integration/test_health_api.py, .planning/phases/07-bundled-runtime-persistent-data/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: When `RM_DATA_DIR` points to an empty stable directory but legacy `apps/backend/data` contains `database.json` and `config.json`, startup preparation copies those files into `RM_DATA_DIR`, writes `.meta/storage-schema.json` with version `1`, and leaves the migrated files readable.
    - Test 2: When a structural migration runs against existing data, backup files are written under `pre-migration/<timestamp>/` before any mutation occurs.
    - Test 3: When the manifest already reports schema version `1`, `prepare_storage()` is idempotent and does not duplicate backups on every restart.
  </behavior>
  <action>Implement `prepare_storage()` in `apps/backend/app/storage_paths.py` so startup migration handles the current legacy layout and future versioned changes per D-08 and D-09. The migration path for Phase 7 must explicitly detect legacy files at `apps/backend/data/database.json`, `apps/backend/data/config.json`, and `apps/backend/data/uploads/`, copy them into the resolved `RM_DATA_DIR` when the stable directory is empty, and write `.meta/storage-schema.json` with `{ "schema_version": 1 }`. Before any migration that changes file structure or content, create a timestamped backup directory under `settings.backup_dir / "pre-migration"` and copy the pre-migration database/config files there. Update `apps/backend/app/main.py` to call `prepare_storage()` during startup before `settings.data_dir.mkdir(...)` or database access. Add `apps/backend/tests/integration/test_desktop_storage.py` covering the three behaviors above using temporary directories and monkeypatched env vars. Keep migration logic lightweight and file-based; do not reset data, prompt the user, or add installer-time behavior.</action>
  <verify>cd apps/backend && uv run --extra dev pytest tests/integration/test_desktop_storage.py -x</verify>
  <acceptance_criteria>
    - `apps/backend/app/storage_paths.py` contains `prepare_storage`
    - `apps/backend/app/storage_paths.py` contains `schema_version`
    - `apps/backend/app/storage_paths.py` contains `pre-migration`
    - `apps/backend/app/main.py` contains `prepare_storage()`
    - `apps/backend/tests/integration/test_desktop_storage.py` contains `test_migrates_legacy_repo_data_into_rm_data_dir`
    - `apps/backend/tests/integration/test_desktop_storage.py` contains `test_prepare_storage_creates_backup_before_structural_migration`
    - `apps/backend/tests/integration/test_desktop_storage.py` contains `test_prepare_storage_is_idempotent_for_current_schema`
  </acceptance_criteria>
  <done>Backend startup prepares a stable AppData-style storage root, performs recoverable migrations, and preserves data across restarts and upgrades.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "RM_DATA_DIR|prepare_storage|schema_version|pre-migration|backup_dir" apps/backend/app/storage_paths.py apps/backend/app/config.py apps/backend/app/main.py`
- [ ] `cd apps/backend && uv run --extra dev pytest tests/integration/test_desktop_storage.py -x`
</verification>

<success_criteria>
The backend persists data and config under a stable per-user directory, creates recoverable backups before structural migration, and keeps the same storage root across upgrades.
</success_criteria>

<output>
After completion, create `.planning/phases/07-bundled-runtime-persistent-data/07-02-SUMMARY.md`
</output>
