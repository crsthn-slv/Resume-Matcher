---
phase: 03-workflow-integrations
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/frontend/app/(default)/tailor/page.tsx
  - apps/frontend/app/(default)/resumes/[id]/page.tsx
  - apps/frontend/lib/api/applications.ts
  - apps/frontend/messages/en.json
  - apps/frontend/messages/es.json
  - apps/frontend/messages/ja.json
  - apps/frontend/messages/pt-BR.json
  - apps/frontend/messages/zh.json
autonomous: true
requirements:
  - TAIL-01
  - TAIL-02
user_setup: []
must_haves:
  truths:
    - After confirming a tailored resume, the user gets an immediate application-creation entry point that carries the new `resume_id` and linked `job_id`.
    - The post-tailoring create flow reuses the resume-linked application pattern from the resume viewer instead of introducing a parallel application screen.
    - Company and role stay manually editable while default status behavior remains backend-driven.
  artifacts:
    - apps/frontend/app/(default)/tailor/page.tsx passes post-tailoring create intent into the normal resume-centric destination.
    - apps/frontend/app/(default)/resumes/[id]/page.tsx recognizes the post-tailoring create intent and opens the linked application creation experience with prefilled identifiers.
    - Locale files include any new CTA, helper, and error strings required by the post-tailoring flow.
  key_links:
    - Resume confirmation still lands on `/resumes/[id]` as the primary destination.
    - Application creation reuses `createApplication` and the Phase 2 resume-viewer form instead of a new route or duplicated form implementation.
---

<objective>
Connect tailoring completion to linked application creation.

Purpose: Make application creation the immediate next step after a tailored resume is confirmed without breaking the existing resume-centric flow.
Output: A post-tailoring entry into the resume viewer's linked-application create flow with `resume_id` and `job_id` carried forward automatically.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-integrations/03-CONTEXT.md
@apps/frontend/app/(default)/tailor/page.tsx
@apps/frontend/app/(default)/resumes/[id]/page.tsx
@apps/frontend/lib/api/applications.ts
@apps/backend/app/routers/applications.py
@apps/frontend/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry post-tailoring application-create intent into the resume-centric destination</name>
  <files>apps/frontend/app/(default)/tailor/page.tsx</files>
  <read_first>apps/frontend/app/(default)/tailor/page.tsx, apps/frontend/app/(default)/resumes/[id]/page.tsx, .planning/phases/03-workflow-integrations/03-CONTEXT.md</read_first>
  <action>Update the tailoring confirm flow so that after a tailored resume is created, the app still routes to `/resumes/[id]` but includes a clear signal that the user came from post-tailoring and should be offered linked application creation immediately. Use a route-safe mechanism that does not break refresh behavior or require a separate page. Preserve the existing fallback behavior when no new `resume_id` is returned.</action>
  <verify>rg -n "resume_id|job_id|router.push|application|create" 'apps/frontend/app/(default)/tailor/page.tsx'</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/tailor/page.tsx` still routes to `/resumes/${newResumeId}` when a tailored resume is confirmed
    - The route transition carries enough information to trigger post-tailoring application creation
    - No standalone application creation route is introduced
  </acceptance_criteria>
  <done>The tailored-resume confirm flow can hand off into application creation without leaving the resume-centric path.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the resume viewer application create flow with prefilled linked identifiers</name>
  <files>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/lib/api/applications.ts</files>
  <read_first>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/lib/api/applications.ts, apps/backend/app/routers/applications.py, .planning/phases/02-dashboard-resume-linked-experience/02-03-SUMMARY.md</read_first>
  <action>Extend the existing resume-viewer application creation path so it can be opened immediately from the post-tailoring handoff. Prefill `resume_id` and `job_id` in the create payload automatically, keep `company` and `role` editable in the form, and continue relying on backend default-status behavior unless the existing UI already presents a valid status picker. Avoid duplicating the form logic into a second post-tailoring component.</action>
  <verify>rg -n "createApplication|resume_id|job_id|company|role|status" 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/lib/api/applications.ts</verify>
  <acceptance_criteria>
    - Resume viewer create flow can open automatically from a post-tailoring entry signal
    - `createApplication` payload includes the current `resume_id`
    - `createApplication` payload includes the linked `job_id` when available
    - Company and role remain editable user inputs in the resume viewer form
  </acceptance_criteria>
  <done>The existing resume-linked application flow now supports immediate post-tailoring creation.</done>
</task>

<task type="auto">
  <name>Task 3: Add post-tailoring CTA, copy, and failure handling without forking the experience</name>
  <files>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/messages/en.json, apps/frontend/messages/es.json, apps/frontend/messages/ja.json, apps/frontend/messages/pt-BR.json, apps/frontend/messages/zh.json</files>
  <read_first>apps/frontend/app/(default)/resumes/[id]/page.tsx, apps/frontend/messages/en.json, .planning/REQUIREMENTS.md, AGENTS.md</read_first>
  <action>Add the UI copy and state transitions needed so the post-tailoring user clearly understands that creating a linked application is the next step. This can be an auto-open dialog, highlighted create section, or comparable immediate prompt inside the resume page, but it must remain part of the normal resume viewer experience. Localize all new copy and handle create failures inline so the user can recover without losing the current resume page context.</action>
  <verify>rg -n "post-tailoring|create application|next step|failed|resume viewer" 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/messages/*.json</verify>
  <acceptance_criteria>
    - The resume viewer presents an immediate create-application prompt when entered from post-tailoring
    - New post-tailoring application strings exist in all locale files modified by the feature
    - Create failures remain on the resume viewer and show a user-visible error state
  </acceptance_criteria>
  <done>The post-tailoring application prompt is obvious, localized, and recoverable on failure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- --file apps/frontend/app/(default)/tailor/page.tsx --file apps/frontend/app/(default)/resumes/[id]/page.tsx --file apps/frontend/lib/api/applications.ts`
- [ ] `npm run format -- --check apps/frontend/app/(default)/tailor/page.tsx apps/frontend/app/(default)/resumes/[id]/page.tsx apps/frontend/lib/api/applications.ts apps/frontend/messages/en.json apps/frontend/messages/es.json apps/frontend/messages/ja.json apps/frontend/messages/pt-BR.json apps/frontend/messages/zh.json`
- [ ] `rg -n "resume_id|job_id|createApplication|router.push|post-tailoring|create application" 'apps/frontend/app/(default)/tailor/page.tsx' 'apps/frontend/app/(default)/resumes/[id]/page.tsx' apps/frontend/lib/api/applications.ts apps/frontend/messages/*.json`
- [ ] Post-tailoring still lands in the normal resume viewer flow instead of a new tracker surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tailoring and application tracking feel like one continuous resume-centric workflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-integrations/03-02-SUMMARY.md`
</output>
