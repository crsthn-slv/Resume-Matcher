---
phase: 03-workflow-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/app/(default)/settings/page.tsx
  - apps/frontend/lib/api/config.ts
  - apps/frontend/messages/en.json
  - apps/frontend/messages/es.json
  - apps/frontend/messages/ja.json
  - apps/frontend/messages/pt-BR.json
  - apps/frontend/messages/zh.json
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
user_setup: []
must_haves:
  truths:
    - Settings exposes the effective application pipeline configuration inside the existing page instead of a new route.
    - Users can add, remove, and reorder statuses inline before saving the updated pipeline.
    - Backend validation errors for statuses still in use are surfaced with actionable context in the Settings UI.
  artifacts:
    - apps/frontend/lib/api/config.ts contains typed helpers for application pipeline config reads and updates.
    - apps/frontend/app/(default)/settings/page.tsx renders a dedicated application pipeline section with add/remove/reorder controls and save state.
    - Locale files include strings for the new Settings pipeline section, validation messages, and affected-application feedback.
  key_links:
    - Settings persistence uses `/config/applications` as the source of truth.
    - Removal failures preserve the backend-provided `affected_applications` payload instead of replacing it with a generic toast.
---

<objective>
Add application pipeline management to the existing Settings page.

Purpose: Give the user one place to inspect and maintain tracker statuses without leaving the established Settings workflow.
Output: A dedicated Settings section for viewing, adding, removing, reordering, and saving application statuses with backend-driven validation feedback.
</objective>

<execution_context>
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cristhian/Downloads/Projetos/Resume Matcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-integrations/03-CONTEXT.md
@apps/frontend/app/(default)/settings/page.tsx
@apps/frontend/lib/api/config.ts
@apps/backend/app/routers/config.py
@apps/frontend/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed frontend config helpers for application pipeline management</name>
  <files>apps/frontend/lib/api/config.ts</files>
  <read_first>apps/frontend/lib/api/config.ts, apps/backend/app/routers/config.py, apps/backend/app/schemas/models.py</read_first>
  <action>Extend the shared config API client with typed request and response helpers for `/config/applications`. The helper layer must cover reading the effective statuses and saving a reordered list, and it must preserve backend error payloads well enough for the Settings UI to surface affected applications when a removal is rejected.</action>
  <verify>rg -n "fetchApplicationsConfig|updateApplicationsConfig|affected_applications|/config/applications" apps/frontend/lib/api/config.ts</verify>
  <acceptance_criteria>
    - `apps/frontend/lib/api/config.ts` contains `export interface ApplicationsConfig`
    - `apps/frontend/lib/api/config.ts` contains `export async function fetchApplicationsConfig`
    - `apps/frontend/lib/api/config.ts` contains `export async function updateApplicationsConfig`
    - Error handling for the update path retains backend `affected_applications` details
  </acceptance_criteria>
  <done>The frontend has a shared typed API surface for application pipeline config.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Settings application pipeline section with add, remove, and reorder controls</name>
  <files>apps/frontend/app/(default)/settings/page.tsx, apps/frontend/messages/en.json, apps/frontend/messages/es.json, apps/frontend/messages/ja.json, apps/frontend/messages/pt-BR.json, apps/frontend/messages/zh.json</files>
  <read_first>apps/frontend/app/(default)/settings/page.tsx, apps/frontend/lib/api/config.ts, .planning/phases/03-workflow-integrations/03-CONTEXT.md, AGENTS.md</read_first>
  <action>Add a dedicated application pipeline section to the existing Settings page. It must load the current statuses, let the user add a new status inline, remove a status inline, and reorder statuses with a clear interaction that fits the existing Swiss-style Settings UI. Keep changes local until the user explicitly saves, and localize all new labels, helper text, empty states, and button text in the existing message files.</action>
  <verify>rg -n "application pipeline|statuses|move up|move down|add status|save" 'apps/frontend/app/(default)/settings/page.tsx' apps/frontend/messages/*.json</verify>
  <acceptance_criteria>
    - `apps/frontend/app/(default)/settings/page.tsx` renders a dedicated application pipeline section
    - The section contains controls to add a status
    - The section contains controls to remove a status
    - The section contains controls to reorder statuses
    - New Settings pipeline strings exist in all locale files modified by the feature
  </acceptance_criteria>
  <done>The user can manage the application pipeline directly from Settings.</done>
</task>

<task type="auto">
  <name>Task 3: Surface backend in-use validation details in the Settings UI</name>
  <files>apps/frontend/app/(default)/settings/page.tsx</files>
  <read_first>apps/frontend/app/(default)/settings/page.tsx, apps/frontend/lib/api/config.ts, apps/backend/app/routers/config.py, .planning/REQUIREMENTS.md</read_first>
  <action>When the backend rejects a pipeline update because statuses are still in use, render the returned context in the Settings section instead of falling back to a generic error. Show enough detail for the user to understand which application records block the removal, while keeping the existing page pattern for error and save-state handling. Do not duplicate backend validation on the client beyond lightweight local guards such as empty-status prevention.</action>
  <verify>rg -n "affected_applications|Cannot remove application statuses|application_id|company|role" 'apps/frontend/app/(default)/settings/page.tsx' apps/frontend/lib/api/config.ts</verify>
  <acceptance_criteria>
    - Rejected pipeline saves render a user-visible error state inside `apps/frontend/app/(default)/settings/page.tsx`
    - The error state includes backend-provided affected application context
    - Client code does not silently drop backend validation details
  </acceptance_criteria>
  <done>The Settings page explains exactly why a status cannot be removed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint -- --file apps/frontend/app/(default)/settings/page.tsx --file apps/frontend/lib/api/config.ts`
- [ ] `npm run format -- --check apps/frontend/app/(default)/settings/page.tsx apps/frontend/lib/api/config.ts apps/frontend/messages/en.json apps/frontend/messages/es.json apps/frontend/messages/ja.json apps/frontend/messages/pt-BR.json apps/frontend/messages/zh.json`
- [ ] `rg -n "fetchApplicationsConfig|updateApplicationsConfig|affected_applications|application pipeline" apps/frontend/lib/api/config.ts 'apps/frontend/app/(default)/settings/page.tsx' apps/frontend/messages/*.json`
- [ ] Settings remains the existing consolidated page and does not introduce a separate application-pipeline route
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings becomes the source of pipeline administration without breaking backend authority
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-integrations/03-01-SUMMARY.md`
</output>
